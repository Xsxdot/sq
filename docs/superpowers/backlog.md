# sq 需求总账

上游需求中枢：先沉淀、想透、再领取。状态流转 `💡 idea → 📋 specced → 🔨 doing → ✅ done`，
`🗄️ shelved` 为显式搁置，`📦 epic` 为大需求的伞行（不可直接领取）。

- `💡 idea` 尚未聊透，需要先走 brainstorming 产出 spec 才可领取。
- `📋 specced` 才是可领取池，领取后交棒 writing-plans。

## Backlog

| ID | 标题 | 状态 | 优先级 | Spec | 原型/流程图 | 验收 | 变更痕迹 | 备注 |
|----|------|------|--------|------|------------|------|---------|------|
| B1 | `produce.Append` 全局锁跨越 fsync，pebble group commit 失效 | ✅ done(已验) | 高 | [spec](specs/2026-08-07-throughput-and-hygiene-design.md) | — | `go test -race ./...` 15 包全绿（macOS+Linux 08-08）；三机基准定律与云机实测回填两份 spec（abb6128 及 M7 spec §吞吐）；无原型/流程图，自动免除对照 08-08 | 08-08 续接 perf 分支收尾：ApplyAsync/Pending.Wait 拆分提交把 fsync 等待挪出队列锁 + AppendBatch 整批落盘 + 默认队列 4→16，写吞吐与队列数脱钩 | 源自 M1 最终整支审查。领于 08-07，plan: [m7-batch1](plans/2026-08-07-m7-batch1-hardening-throughput-hygiene.md)、[produce-throughput](plans/2026-08-08-produce-throughput-optimization.md) |
| B2 | `store.NewBatch` 返回裸 `*pebble.Batch`，「唯一写入口」无编译期强制 | ✅ done(已验) | 高 | [spec](specs/2026-08-08-sq-v2-replication-design.md) | — | `go test -race ./...` 全绿（15 包）+ `grep pebble.Batch` 零泄漏——08-10 合入 main 后复验：全主模块 `pebble.Batch` 仅出现在 `internal/store/store.go`（3 处，均为封装内部），`NewBatch()` 返回 `*store.Batch` 包装类型，无人能绕过拦截点拿到裸批次；无原型/流程图，自动免除对照 | 08-08 纳入 V2(B8)：「唯一写入口」编译期强制即 raft 拦截点地基（V2 spec §4）；写入点已从 M1 时的 6 处涨至 34 处/10 文件，越晚越贵 | 源自 M1 最终整支审查。属 B8（历史 ID 保留不改号）。领于 08-08，plan: [v2-batch1](plans/2026-08-08-v2-batch1-replication-foundation.md) |
| B3 | receipt handle 加签，并补 `ReceiveMessage`/`AckMessage` 的 topic 校验 | ✅ done(已验) | 中 | [spec](specs/2026-08-07-auth-and-protocol-hardening-design.md) | — | `go test -race ./...` 全绿 + e2e 全量 ok 428.6s（Linux 08-08，含 HMAC handle 与 topic/queueID 校验用例）；无原型/流程图，自动免除对照 08-08 | — | 源自 M1 最终整支审查。独立持久化密钥（`meta/handle_secret`），鉴权关闭时防伪造也生效。领于 08-07，plan: [m7-batch1](plans/2026-08-07-m7-batch1-hardening-throughput-hygiene.md) |
| B4 | `test/e2e` 拆为独立 Go module | ✅ done(已验) | 中 | [spec](specs/2026-08-07-throughput-and-hygiene-design.md) | — | e2e 独立 module 后主套件与 e2e 均绿（Linux 08-08：主套件 15 包 -race ok，e2e ok 428.6s）；无原型/流程图，自动免除对照 08-08 | — | 源自 M1 最终整支审查。主模块 `go install` 不再拉 SDK 间接依赖。领于 08-07，plan: [m7-batch1](plans/2026-08-07-m7-batch1-hardening-throughput-hygiene.md) |
| B5 | 补两条承重测试：`Scan(limit<=0)` 与并发 `Append` 的 `-race` | ✅ done(已验) | 中 | [spec](specs/2026-08-07-throughput-and-hygiene-design.md) | — | 两条测试入库并随 `-race` 套件全绿（macOS+Linux 08-08）；无原型/流程图，自动免除对照 08-08 | — | 源自 M1 最终整支审查。领于 08-07，plan: [m7-batch1](plans/2026-08-07-m7-batch1-hardening-throughput-hygiene.md) |
| B6 | 混 topic 批次可能在部分落盘后返回不可重试错误 | ✅ done(已验) | 低 | [spec](specs/2026-08-07-auth-and-protocol-hardening-design.md) | — | `SendMessage` 第零遍只读预检入库，rpc 包 -race 20.1s ok（Linux 08-08）；无原型/流程图，自动免除对照 08-08 | — | 源自 M1 最终修复波自查。预检保持无副作用（不 EnsureTopic）。领于 08-07，plan: [m7-batch1](plans/2026-08-07-m7-batch1-hardening-throughput-hygiene.md) |
| B7 | M7：v1.0 发布打磨 | 📦 epic | 高 | — | — | — | — | 08-07 立项定范围：B7.1 多组 AK/SK + B7.2 发布打磨 + 纳入 B1/B3/B4/B5/B6（历史 ID 保留不改号）。进度 6/7，仅剩 B7.2。场景测试计划（plans/2026-08-07-scenario-test.md）独立于 M7 另行执行 |
| B7.1 | 多组 AK/SK | ✅ done(已验) | 高 | [spec](specs/2026-08-07-auth-and-protocol-hardening-design.md) | — | 凭据 list + 严格解析（a4c6e5f：旧 `access_key`/`secret_key` 标量硬报错并给迁移提示）入库，config/rpc 测试随套件全绿（08-08）；无原型/流程图，自动免除对照 08-08 | — | 属 B7。破坏性配置变更已在 v1.0 前窗口完成。领于 08-07，plan: [m7-batch1](plans/2026-08-07-m7-batch1-hardening-throughput-hygiene.md) |
| B7.2 | 发布打磨：docker 镜像、systemd 单元、快速开始、文档总校 | 💡 idea | 高 | — | — | — | — | 属 B7。spec §11 M7 原定内容。放最后做：配置格式与全部能力定稿后只付一次文档成本。注意与 B8（V2）的排序：V2 改协议面与部署形态，文档总校宜在 V2 集成后 |
| B8 | V2：复制层（三节点主备） | 📦 epic | 高 | — | — | — | — | 08-08 立项定范围：纳入 B2（拦截点地基，历史 ID 保留）+ B8.1 spike + B8.2 集群集成 + B8.3 集群场景测试。分片外溢为 B9(V3)。开工前提（已满足）：M7 与写吞吐优化落地 main。**08-10 收口：进度 4/4，B2 / B8.1 / B8.2 / B8.3 全部 ✅ done(已验)，五个批次（batch①地基→②复制内核→③协议面接线→④快照/截断/扩容→⑤场景测试/读屏障/控制台）连同 P0 修复（B10）已全部合入 main**。V2 范围内无未完项；实测暴露但**不在 V2 范围**的两件事各自独立排期：B11（全集群硬宕后从本地日志自恢复）、B7.2（发布打磨与文档总校，需在 V2 定稿后做） |
| B8.1 | etcd/raft 薄壳 spike：最小三节点原型 + 切换/吞吐/追齐实测标定 | ✅ done(已验) | 高 | [spec](specs/2026-08-08-sq-v2-replication-design.md) | — | spike 套件 `-race` 全绿 + 三组数字：切换 1.47~1.53s（`TestKillLeaderFailover`）/ 吞吐两档矩阵（`BenchmarkProposeQuorumFsync`/`Mem`，本机 M1 Pro + Linux 2vCPU，见 spec §2.3）/ 追齐 ≈283ms（`TestUncleanRestartRejoinsAsLearner`） | — | 属 B8。spec §8.1：独立 module 防依赖入主图；产出选举耗时、两档刷盘吞吐、learner 追齐数据回填 spec §2.3/§6。领于 08-08，plan: [v2-batch1](plans/2026-08-08-v2-batch1-replication-foundation.md) |
| B8.2 | 集群模式集成：复制接口、组管理、QueryRoute 路由、切换语义、单机→集群升级 | ✅ done(已验) | 高 | [spec](specs/2026-08-08-sq-v2-replication-design.md) | — | 08-10 合入 main 后整支复验：macOS `go test ./...` 17/17 包 ok（`internal/cluster` 69.6s、`internal/rpc` 26.2s）+ e2e 全量 ok。Linux 双机 e2e 22/22 PASS（联想 4 核 + 阿里云 2 核，三节点 108s / 集群 DLQ 162s / 单机→单节点升级 16s / 扩容 5s / 截断有界 4s），远端零 Go 工具链、交叉编译 broker 经 `SQ_E2E_BROKER` 注入；主套件原生 `-race` exit 0（阿里云 2 vCPU，1:39.63，峰值 RSS 424MB）；386/arm 交叉编译通过。**真机三节点实证**（三台阿里云各一节点）：`/admin/cluster` 的 `self`/`is_leader`/`peers` 跨节点自洽，follower 写转发与 leader 归属一致。跨机写吞吐 3,780/4,509/6,865 msg/s（并发 16/64/256，quorum-mem，失败 0）。无原型/流程图（控制台集群页归 B8.3），自动免除对照 | 08-08 batch②（复制内核）完成：internal/cluster + internal/replication 入库，三节点集成 -race 全绿（macOS+Linux）。08-09 batch③（协议面接线）完成：main 集群装配（ErrUncleanShutdown→Rejoin 自愈）、RouteView 指向组 leader、ForwardAppend/ForwardApply 控制通道、PrepareJoin 重入编排与自动升 voter、单机→单节点集群升级用例、三节点 e2e（kill 数据组 leader→auto-rejoin→升 voter，270 条零丢失）；修复 pebble 批次池内存别名覆写 raft 日志根因（`NewBatchFromRepr` 深拷贝，评审 R0 三节点 e2e 根因）。08-09 batch④（快照+截断+扩容）完成：快照链路（ReadView 一致性视图 + 安装侧清空重来）、周期日志截断（applied−RetainEntries，leader 受 minMatch 下界，并带逃生门：learner 不入下界 + 绝对上限，死节点不再把日志钉死）、单机→多节点扩容 Join（NoBootstrap 空白启动 + BootstrapVoters 种子引导）、三处根因修复（NoBootstrap 撞车 / BootstrapVoters 传输层静默丢弃 / truncateLoop-Done 竞态）、leadership 迁移写屏障（写钩子先于 IsLeader 可见）、ConfChange applied 缺口（ConfState 随 ApplyConfChange 持久化，`raft/<g>/conf`）、rd.MustSync 精准落盘（约 2x 时延收益）、e2e 证据（扩容 200 条快照追齐零丢失 + 截断键数有界；Linux -race 全量绿）。08-10 两轮评审修复入库：快照键族枚举按字节序排序 + 区间两两不相交断言（续扫单调性与块间互斥成强制不变量，堵死多键族死循环/漏发/同键重发）、snapRegistry 借用计数 + 借出续期 + 硬 TTL（GC 不回收在用视图，大库传输不活锁，久悬视图仍可回收）、快照安装失败改 fail-stop + leader 侧停滞检测（ReportSnapshot 让对端回 Probe 重发，不再静默内存/磁盘分叉）、拉取侧游标单调/块数/字节三道守卫、安装期 Step 非阻塞、Join 拒绝判据收窄为「前 raft 期存量标记 + FSM 非空 + 日志未压缩」三者合取（`raft/preraft`，全新集群不再被误拒）、快照视图 TTL 提为配置项 `cluster.snapshot_view_ttl`、控制通道 OpPing=0 保留（探活止步 Manager 自装层，不再穿透业务 handler）。Linux 双机 e2e 全量绿（联想 4 核 + 阿里云 2 核，22/22 PASS：三节点 108s、集群 DLQ 162s、单机→单节点升级 16s、扩容 5s、截断有界 4s）——远端零 Go 工具链，交叉编译 broker 后经 `SQ_E2E_BROKER` 注入（harness 早有此入口，此前误判为「Linux e2e 跑不了」）。08-10 终审第三轮 5 条（判活按 peer 定向台账、安装重试改 2min 时间窗、在途借用判活、maxSnapshotBytes 32 位溢出、fail-stop 恢复路径注释）全部修复：macOS 全绿 + `-race` 全绿，联想 4 核与阿里云 2 核双 Linux 主机 PASS，386/arm 交叉编译通过。08-10 首次端到端写吞吐实测（`test/e2e/sdk_cluster_bench_test.go`，SDK→gRPC→raft→quorum，128B/24 队列/三进程同机）：集群 quorum-mem 3149(2核)/6224(4核) msg/s，quorum-fsync 2111(云SSD)/579(慢盘)，同机单机基线 8422/15221；quorum-mem 为 CPU-bound（核数翻倍吞吐翻倍），quorum-fsync 完全由盘 fsync 延迟决定。跨机三节点未测（只有 2 台可用机器，不给推断数字）。 | 属 B8。spec §3–§7。开工前提：B8.1 数据回填完成，复制接口形状以 spike 实证为准。batch④ plan 已写（08-09，[v2-batch4](plans/2026-08-09-v2-batch4-snapshot-truncation-expansion.md)，10 task）：快照 + 日志截断、单机→多节点扩容、leadership 迁移写屏障（batch③ 评审 m1）、ConfChange applied 键缺口、首轮 leader 日志 term=0、rd.MustSync、confStateFromEntries 多变更守卫、goroutine 泄漏。batch④ **显式外推 batch⑤** 的：B8.3 集群场景测试（kill -9 / 分区 / 滚动重启 / 断电 + 毒消息→DLQ + producer 提交后立退）、admin 控制台集群视图与 follower 写转发 UX、read-index/apply barrier 完整读屏障 |
| B8.3 | 集群场景测试：kill-leader、网络分区、滚动重启、断电模拟 | ✅ done(已验) | 中 | [spec](specs/2026-08-08-sq-v2-replication-design.md) | [原型](../../prototypes/base/pages/cluster.html)（集群页，已与真实页面对照回流 base） | 08-10 batch⑤ 八条场景用例全 PASS（`TestScenario*`：kill -9 leader / 少数派不可写 / 滚动重启 / 全集群断电无法自愈 / 断电 quorum 丢失再恢复 / 毒消息→DLQ / producer 提交后立退 / admin follower 写转发）。**Linux 三条证据线**（08-10）：①主套件交叉编译 17/17 包 ok（联想 4 核，远端零 Go 工具链）②主套件原生 `-race` exit 0、1:39.63、峰值 RSS 424MB（阿里云 2 vCPU，go1.26.1）③e2e 全量 exit 0、18:06.05、峰值 RSS 451MB（联想 4 核，0 个 FAIL）。前端 11 个测试文件 38 条 PASS。**真机三节点实证**（三台阿里云各一节点、内网 172.19.25.178/179/180）：`GET /admin/cluster` 的 `self`/`is_leader`/`peers` 三节点自洽，`peers` 只在该组 leader 上有数据（即控制台「复制进度仅在本节点为该组 leader 时可见」的事实依据）；follower 写转发成立，转发与否与 `/admin/cluster` 报的 leader 归属一致。跨机写吞吐（`TestExternalClusterWriteThroughput`，quorum-mem、20000×128B、客户端与节点1 共置）：并发 16/64/256 → 3,780 / 4,509 / 6,865 msg/s，失败 0，客户端峰值 RSS 120MB | 08-10 batch⑤ 入库（17 提交）：组读屏障原语 + `Manager.ReadBarrier` 批合并 + 接入 `deliver.Receive`；`procCluster` 进程级故障注入 harness（kill/优雅停/原地重启/假死）+ 八条场景用例；`GET /admin/cluster` + `GroupView.Commit` + 控制台集群页（待 apply = commit−applied）+ follower 写转发 UX；原型基准站集群页归位 `pages/` 并全站补入口。另修 `df6848d`：吞吐基准用 `atomic.Value` 记首错，撞多类型 error 会 panic 掉整个基准进程、连已跑完的档位一起丢 | 属 B8。spec §8.2。**订正原措辞**：`test/scenario/` 框架计划（2026-08-07）从未执行，本批不去补它——场景测试长在既有 `test/e2e` 三节点进程级 harness 上（真 broker 进程 + 真 SDK + 真 gRPC），证据更强路径更短。统一不变量对账用确认集（Send 成功即登记，断言全量被消费，允许重复不允许丢失）。batch④（08-09）显式外推三项已全部落地：①本项四类场景 + 毒消息→DLQ + producer 提交后立退 ②admin 控制台集群视图与 follower 写转发 UX ③read-index/apply barrier 完整读屏障。本批实测暴露的 wipe 顺序 P0 见 **B10**（事实只维护一份，此处不复述）；`TestScenarioFullPowerLossCannotRecover` 已随 B10 修复改写为断言「拒启且数据目录保留」。**未测出共置税**：跨机数字有了，但共置矩阵（一台机三 broker 进程）在 2 vCPU/1.6GB 机上因内存不足未跑完，缺同硬件对照，故跨机数字只能单独看 |
| B10 | 不干净关机自愈在取得集群接纳前销毁数据（`Rejoin` wipe 顺序倒置） | ✅ done(已验) | 高(P0) | — | — | `TestRejoinKeepsDataWhenClusterUnreachable` 入库（对端全不可达时 Rejoin 报错 + 数据目录可重开且旧键仍在）；macOS `go test ./...` 全绿；Linux（联想 4 核，6.17.0-29-generic x86_64）`-test.run "Rejoin\|Unclean"` PASS 13.4s、新用例单跑 PASS 3.16s。无原型/流程图，自动免除对照 | 08-10 修复入库（b3cd61e，分支 v2-fix-rejoin-wipe-order）：`Rejoin` 第 2/3 步对调，PrepareJoin 取得接纳后才 WipeForRejoin；编排失败时数据分毫未动，main 改为拒启并在错误串与日志明写恢复途径；harness `rejoinAsLearner` 同步对调 | 08-10 由 batch⑤ 场景四（三节点同时断电）实测暴露、追根因确认。原顺序是「先清空再求接纳」，而 main 在任何 `ErrUncleanShutdown`（kill -9 / OOM / 断电 / 快照安装 fail-stop panic）后无人值守自动走这条路：全集群断电 = 三份副本各自清空后都找不到 leader，永久全损；2-of-3 硬宕 = 两节点清空后再也凑不齐批准自己 ConfChange 的 quorum，一次可恢复事件变成集群永久死亡。`prepareJoinPoll` 只做对外控制调用、不碰 dataDir，两步交换无技术障碍——原顺序是纯设计失误。用户拍板插队修，先于 batch⑤ 收尾。遗留半边由 **B11** 收口（08-10）：能证明本地日志完好时直接原身份恢复，B10 的两条顺序红线断言搬至 `TestScenarioRebootedMemNeedsPermit` 继续生效 |
| B11 | 不干净关机的节点无法从本地日志自恢复，全集群硬宕后集群拒启 | 🔨 doing | 高 | [spec](specs/2026-08-10-unclean-local-recovery-design.md) | — | macOS（08-11）：`go test -race ./...` 17/17 包全绿；e2e 全量 1102.2s、31 PASS、0 FAIL（`TestScenario*` 场景新增 2 PASS + 2 SKIP）；单测五条恢复分支 + 抬 term 四格 + 安全门守卫 + 签字往返全 PASS；Linux 双平台验收待本地补测（macOS 执行环境无 Linux 工具链） | 08-10/08-11 五分支本地恢复落地（分支 v2-b11-unclean-local-recovery）：机器世代读取（Linux boot_id / darwin kern.boottime / 其它平台保守拒读）、raft/bootgen 键、五分支判定纯函数、一次性许可 + ForceLocalRecover、NewManager 接线（local-resume / local-forced 复用 buildGroup clean 分支）、sq recover 子命令、mem 档 local-resume 也抬 term（实现期订正，Pebble NoSync 是异步的）、e2e 四条场景 + restartAll 整批重启 | **状态订正（08-11 审核）**：代码全部完成并在分支 `v2-b11-unclean-local-recovery` 上，但**不能记 done——spec §11 验收第 2 条（静置 ≥500ms 后确认的消息零丢失）实测不成立**，对应两条 e2e 场景现为 skip。四条验收里两条过、两条挂在 B11-OPEN-1 上，「验收通过」与「用例被 skip 掉」不能同时为真。合不合 main 是另一个决定（见 B11-OPEN-1 备注里的取舍），未合并前保持 doing。B10 的遗留半边。发现新缺陷 **B11-OPEN-1**（mem 档本地恢复丢已确认消息，见下），已留痕待排期。**Task 7/8 实跑中与 plan 预期的偏差见 notes/2026-08-11-b11-grant-path-loss.md**：local-resume 与 local-forced 两条 mem 路径都丢健康期尾部已确认消息（非仅 grant 路径），fsync 档与拒启路径不丢 |
| B11-OPEN-1 | mem 档本地恢复（local-resume / local-forced）丢失健康期尾部已确认消息 | 💡 idea | 高 | — | — | — | — | 08-11 B11 Task 7 实跑暴露（证据：notes/2026-08-11-b11-grant-path-loss.md）。`TestScenarioFullProcessCrashRecoversLocally` 与 `TestScenarioRebootedMemRecoversAfterGrant` 在静置 ≥500ms 后 SIGKILL 三节点，恢复后各丢 17 / 19–20 条健康期尾部已确认消息（`消息已写入` 已打、恢复读回 raft 日志尾比已确认数少）；fsync 档与拒启路径同条件不丢。优先级给「高」：已确认消息在无人值守的本地恢复路径上丢失 = 数据正确性问题，与 B10 同级，且 B11 的「静置后零丢失可断言」前提因此不成立。**待定的第一个问题：这是 B11 引入的，还是 B11 暴露的？** B11 之前三节点全硬宕后集群整个起不来，这批消息同样不在盘上，只是没人能观察到；若属后者，B11 的净效果是把「响亮的拒启」换成了「安静地少几条」——这个取舍要不要接受，决定 B11 能不能先合。根因与这个判断由审核者接手，接手点见 notes 第 7 节的三条未排除假设 |
| B9 | V3：分片集群（topic/queue 分片到多 Raft 组，横向扩展写吞吐） | 💡 idea | 低 | — | — | — | — | 源自 V2 brainstorm（spec §1 非目标表外溢）。前提：B8 的复制组抽象已把每分片=一个复制组的结构预留好，届时只加路由层。单机 57k msg/s 实测在前，无近期压力 |
