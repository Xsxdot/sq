# sq 需求总账

上游需求中枢：先沉淀、想透、再领取。状态流转 `💡 idea → 📋 specced → 🔨 doing → ✅ done`，
`🗄️ shelved` 为显式搁置，`📦 epic` 为大需求的伞行（不可直接领取）。

- `💡 idea` 尚未聊透，需要先走 brainstorming 产出 spec 才可领取。
- `📋 specced` 才是可领取池，领取后交棒 writing-plans。

## Backlog

| ID | 标题 | 状态 | 优先级 | Spec | 原型/流程图 | 验收 | 变更痕迹 | 备注 |
|----|------|------|--------|------|------------|------|---------|------|
| B1 | `produce.Append` 全局锁跨越 fsync，pebble group commit 失效 | ✅ done(已验) | 高 | [spec](specs/2026-08-07-throughput-and-hygiene-design.md) | — | `go test -race ./...` 15 包全绿（macOS+Linux 08-08）；三机基准定律与云机实测回填两份 spec（abb6128 及 M7 spec §吞吐）；无原型/流程图，自动免除对照 08-08 | 08-08 续接 perf 分支收尾：ApplyAsync/Pending.Wait 拆分提交把 fsync 等待挪出队列锁 + AppendBatch 整批落盘 + 默认队列 4→16，写吞吐与队列数脱钩 | 源自 M1 最终整支审查。领于 08-07，plan: [m7-batch1](plans/2026-08-07-m7-batch1-hardening-throughput-hygiene.md)、[produce-throughput](plans/2026-08-08-produce-throughput-optimization.md) |
| B2 | `store.NewBatch` 返回裸 `*pebble.Batch`，「唯一写入口」无编译期强制 | 🔨 doing | 高 | [spec](specs/2026-08-08-sq-v2-replication-design.md) | — | `go test -race ./...` 全绿（15 包）+ `grep pebble.Batch` 零泄漏（主模块除 store 外无人触及） | 08-08 纳入 V2(B8)：「唯一写入口」编译期强制即 raft 拦截点地基（V2 spec §4）；写入点已从 M1 时的 6 处涨至 34 处/10 文件，越晚越贵 | 源自 M1 最终整支审查。属 B8（历史 ID 保留不改号）。领于 08-08，plan: [v2-batch1](plans/2026-08-08-v2-batch1-replication-foundation.md) |
| B3 | receipt handle 加签，并补 `ReceiveMessage`/`AckMessage` 的 topic 校验 | ✅ done(已验) | 中 | [spec](specs/2026-08-07-auth-and-protocol-hardening-design.md) | — | `go test -race ./...` 全绿 + e2e 全量 ok 428.6s（Linux 08-08，含 HMAC handle 与 topic/queueID 校验用例）；无原型/流程图，自动免除对照 08-08 | — | 源自 M1 最终整支审查。独立持久化密钥（`meta/handle_secret`），鉴权关闭时防伪造也生效。领于 08-07，plan: [m7-batch1](plans/2026-08-07-m7-batch1-hardening-throughput-hygiene.md) |
| B4 | `test/e2e` 拆为独立 Go module | ✅ done(已验) | 中 | [spec](specs/2026-08-07-throughput-and-hygiene-design.md) | — | e2e 独立 module 后主套件与 e2e 均绿（Linux 08-08：主套件 15 包 -race ok，e2e ok 428.6s）；无原型/流程图，自动免除对照 08-08 | — | 源自 M1 最终整支审查。主模块 `go install` 不再拉 SDK 间接依赖。领于 08-07，plan: [m7-batch1](plans/2026-08-07-m7-batch1-hardening-throughput-hygiene.md) |
| B5 | 补两条承重测试：`Scan(limit<=0)` 与并发 `Append` 的 `-race` | ✅ done(已验) | 中 | [spec](specs/2026-08-07-throughput-and-hygiene-design.md) | — | 两条测试入库并随 `-race` 套件全绿（macOS+Linux 08-08）；无原型/流程图，自动免除对照 08-08 | — | 源自 M1 最终整支审查。领于 08-07，plan: [m7-batch1](plans/2026-08-07-m7-batch1-hardening-throughput-hygiene.md) |
| B6 | 混 topic 批次可能在部分落盘后返回不可重试错误 | ✅ done(已验) | 低 | [spec](specs/2026-08-07-auth-and-protocol-hardening-design.md) | — | `SendMessage` 第零遍只读预检入库，rpc 包 -race 20.1s ok（Linux 08-08）；无原型/流程图，自动免除对照 08-08 | — | 源自 M1 最终修复波自查。预检保持无副作用（不 EnsureTopic）。领于 08-07，plan: [m7-batch1](plans/2026-08-07-m7-batch1-hardening-throughput-hygiene.md) |
| B7 | M7：v1.0 发布打磨 | 📦 epic | 高 | — | — | — | — | 08-07 立项定范围：B7.1 多组 AK/SK + B7.2 发布打磨 + 纳入 B1/B3/B4/B5/B6（历史 ID 保留不改号）。进度 6/7，仅剩 B7.2。场景测试计划（plans/2026-08-07-scenario-test.md）独立于 M7 另行执行 |
| B7.1 | 多组 AK/SK | ✅ done(已验) | 高 | [spec](specs/2026-08-07-auth-and-protocol-hardening-design.md) | — | 凭据 list + 严格解析（a4c6e5f：旧 `access_key`/`secret_key` 标量硬报错并给迁移提示）入库，config/rpc 测试随套件全绿（08-08）；无原型/流程图，自动免除对照 08-08 | — | 属 B7。破坏性配置变更已在 v1.0 前窗口完成。领于 08-07，plan: [m7-batch1](plans/2026-08-07-m7-batch1-hardening-throughput-hygiene.md) |
| B7.2 | 发布打磨：docker 镜像、systemd 单元、快速开始、文档总校 | 💡 idea | 高 | — | — | — | — | 属 B7。spec §11 M7 原定内容。放最后做：配置格式与全部能力定稿后只付一次文档成本。注意与 B8（V2）的排序：V2 改协议面与部署形态，文档总校宜在 V2 集成后 |
| B8 | V2：复制层（三节点主备） | 📦 epic | 高 | — | — | — | — | 08-08 立项定范围：纳入 B2（拦截点地基，历史 ID 保留）+ B8.1 spike + B8.2 集群集成 + B8.3 集群场景测试。分片外溢为 B9(V3)。开工前提（已满足）：M7 与写吞吐优化落地 main |
| B8.1 | etcd/raft 薄壳 spike：最小三节点原型 + 切换/吞吐/追齐实测标定 | 🔨 doing | 高 | [spec](specs/2026-08-08-sq-v2-replication-design.md) | — | spike 套件 `-race` 全绿 + 三组数字：切换 1.47~1.53s（`TestKillLeaderFailover`）/ 吞吐两档矩阵（`BenchmarkProposeQuorumFsync`/`Mem`，本机 M1 Pro + Linux 2vCPU，见 spec §2.3）/ 追齐 ≈283ms（`TestUncleanRestartRejoinsAsLearner`） | — | 属 B8。spec §8.1：独立 module 防依赖入主图；产出选举耗时、两档刷盘吞吐、learner 追齐数据回填 spec §2.3/§6。领于 08-08，plan: [v2-batch1](plans/2026-08-08-v2-batch1-replication-foundation.md) |
| B8.2 | 集群模式集成：复制接口、组管理、QueryRoute 路由、切换语义、单机→集群升级 | 🔨 doing | 高 | [spec](specs/2026-08-08-sq-v2-replication-design.md) | — | — | 08-08 batch②（复制内核）完成：internal/cluster + internal/replication 入库，三节点集成 -race 全绿（macOS+Linux）。08-09 batch③（协议面接线）完成：main 集群装配（ErrUncleanShutdown→Rejoin 自愈）、RouteView 指向组 leader、ForwardAppend/ForwardApply 控制通道、PrepareJoin 重入编排与自动升 voter、单机→单节点集群升级用例、三节点 e2e（kill 数据组 leader→auto-rejoin→升 voter，270 条零丢失）；修复 pebble 批次池内存别名覆写 raft 日志根因（`NewBatchFromRepr` 深拷贝，评审 R0 三节点 e2e 根因）。08-09 batch④（快照+截断+扩容）完成：快照链路（ReadView 一致性视图 + 安装侧清空重来）、周期日志截断（applied−RetainEntries，leader 受 minMatch 下界）、单机→多节点扩容 Join（NoBootstrap 空白启动 + BootstrapVoters 种子引导）、三处根因修复（NoBootstrap 撞车 / BootstrapVoters 传输层静默丢弃 / truncateLoop-Done 竞态）、leadership 迁移写屏障（写钩子先于 IsLeader 可见）、ConfChange applied 缺口（ConfState 随 ApplyConfChange 持久化，`raft/<g>/conf`）、rd.MustSync 精准落盘（约 2x 时延收益）、e2e 证据（扩容 200 条快照追齐零丢失 + 截断键数有界；Linux -race 全量绿） | 属 B8。spec §3–§7。开工前提：B8.1 数据回填完成，复制接口形状以 spike 实证为准。batch④ plan 已写（08-09，[v2-batch4](plans/2026-08-09-v2-batch4-snapshot-truncation-expansion.md)，10 task）：快照 + 日志截断、单机→多节点扩容、leadership 迁移写屏障（batch③ 评审 m1）、ConfChange applied 键缺口、首轮 leader 日志 term=0、rd.MustSync、confStateFromEntries 多变更守卫、goroutine 泄漏。batch④ **显式外推 batch⑤** 的：B8.3 集群场景测试（kill -9 / 分区 / 滚动重启 / 断电 + 毒消息→DLQ + producer 提交后立退）、admin 控制台集群视图与 follower 写转发 UX、read-index/apply barrier 完整读屏障。08-10 batch⑤ 落地：read-index/apply barrier 完整读屏障（`Manager.ReadBarrier` 排队成批合流 + `read_barrier` 配置开关 + 消费读路径接入）、`GET /admin/cluster` 集群拓扑视图 + 控制台集群页（成员表 / 待 apply（commit−applied）/ leader 侧复制进度）、follower 写转发 UX（`handleMessageSend` 经 ForwardAppend 转发并如实标记 forwarded）——详见 B8.3 行与 plans/2026-08-10-v2-batch5-scenario-readbarrier-console.md |
| B8.3 | 集群场景测试：kill-leader、网络分区、滚动重启、断电模拟 | ✅ done | 中 | [spec](specs/2026-08-08-sq-v2-replication-design.md) | — | 08-10 batch⑤ 六条场景 PASS（macOS quorum-mem）：kill -9 leader / 少数派不可写 / 滚动重启 / 断电多数派存活 / 毒消息→DLQ / producer 提交后立退 + admin 写转发 e2e；Linux 双机验证未跑需回本机补 | — | 属 B8。spec §8.2。**订正原措辞**：`test/scenario/` 框架计划（2026-08-07）从未执行，本批不去补它——场景测试长在既有 `test/e2e` 三节点进程级 harness 上（真 broker 进程 + 真 SDK + 真 gRPC），证据更强路径更短。统一不变量对账用确认集（Send 成功即登记，断言全量被消费，允许重复不允许丢失）。**P0 实测发现（本批暴露，修复不在 batch⑤ 范围）：「不干净关机自愈会在确认可恢复前销毁数据」。** 步骤序：`cluster.Rejoin` 是「第 2 步 WipeForRejoin 清空数据目录 → 第 3 步 PrepareJoin 找 leader」（manager.go），且 `cmd/sq/main.go` 在任何 ErrUncleanShutdown 后无人值守自动走这条路——节点在确认自己能被接纳之前就把本地唯一一份数据毁了。后果一：全集群断电 = 三份数据全部先后被清空、且无 leader 可重入、进程退出——不是「不可用」，是永久全损。后果二：2-of-3 硬宕也一样（两个节点 wipe 后无法重入：幸存单节点给不出批准成员变更的 quorum），集群从一次可恢复事件变成永久死亡。修法（不在本批范围）：改为「先确认 leader 可达并愿意接纳、再 wipe；编排不成就保留数据拒启告警」。对应测试如实记录该行为：`TestScenarioFullPowerLossCannotRecover` 断言「判定不干净 → 重入编排失败 → 进程无法提供服务」的机制（wipe 顺序修复后它会红——那正是信号）；`TestScenarioPowerLossQuorumLostThenRestored`（断电时优雅停 + kill -9 混合、1 of 3 无 quorum，先重启优雅停节点重建多数派、再让不干净节点重入——覆盖干净关机本地日志回归与不干净 Rejoin 两条恢复路混合）为互补绿路径。batch④（08-09）显式外推三项已全部落地：①本项四类场景 + 毒消息→DLQ + producer 提交后立退 ②admin 控制台集群视图与 follower 写转发 UX ③read-index/apply barrier 完整读屏障 |
| B9 | V3：分片集群（topic/queue 分片到多 Raft 组，横向扩展写吞吐） | 💡 idea | 低 | — | — | — | — | 源自 V2 brainstorm（spec §1 非目标表外溢）。前提：B8 的复制组抽象已把每分片=一个复制组的结构预留好，届时只加路由层。单机 57k msg/s 实测在前，无近期压力 |
