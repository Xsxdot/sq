# sq 需求总账

上游需求中枢：先沉淀、想透、再领取。状态流转 `💡 idea → 📋 specced → 🔨 doing → ✅ done`，
`🗄️ shelved` 为显式搁置，`📦 epic` 为大需求的伞行（不可直接领取）。

- `💡 idea` 尚未聊透，需要先走 brainstorming 产出 spec 才可领取。
- `📋 specced` 才是可领取池，领取后交棒 writing-plans。

## Backlog

| ID | 标题 | 状态 | 优先级 | Spec | 原型/流程图 | 验收 | 变更痕迹 | 备注 |
|----|------|------|--------|------|------------|------|---------|------|
| B1 | `produce.Append` 全局锁跨越 fsync，pebble group commit 失效 | ✅ done(已验) | 高 | [spec](specs/2026-08-07-throughput-and-hygiene-design.md) | — | `go test -race ./...` 15 包全绿（macOS+Linux 08-08）；三机基准定律与云机实测回填两份 spec（abb6128 及 M7 spec §吞吐）；无原型/流程图，自动免除对照 08-08 | 08-08 续接 perf 分支收尾：ApplyAsync/Pending.Wait 拆分提交把 fsync 等待挪出队列锁 + AppendBatch 整批落盘 + 默认队列 4→16，写吞吐与队列数脱钩 | 源自 M1 最终整支审查。领于 08-07，plan: [m7-batch1](plans/2026-08-07-m7-batch1-hardening-throughput-hygiene.md)、[produce-throughput](plans/2026-08-08-produce-throughput-optimization.md) |
| B2 | `store.NewBatch` 返回裸 `*pebble.Batch`，「唯一写入口」无编译期强制 | ✅ done(已验) | 高 | [spec](specs/2026-08-08-sq-v2-replication-design.md) | — | `go test -race ./...` 全绿（15 包）+ `grep pebble.Batch` 零泄漏——08-10 合入 main 后复验：全主模块 `pebble.Batch` 仅出现在 `internal/store/store.go`（3 处，均为封装内部），`NewBatch()` 返回 `*store.Batch` 包装类型，无人能绕过拦截点拿到裸批次；无原型/流程图，自动免除对照 | 08-08 纳入 V2(B8)：「唯一写入口」编译期强制即 raft 拦截点地基（V2 spec §4）；写入点已从 M1 时的 6 处涨至 34 处/10 文件，越晚越贵 | 源自 M1 最终整支审查。属 B8（历史 ID 保留不改号）。领于 08-08，plan: [v2-batch1](plans/2026-08-08-v2-batch1-replication-foundation.md) |
| B3 | receipt handle 加签，并补 `ReceiveMessage`/`AckMessage` 的 topic 校验 | ✅ done(已验) | 中 | [spec](specs/2026-08-07-auth-and-protocol-hardening-design.md) | — | `go test -race ./...` 全绿 + e2e 全量 ok 428.6s（Linux 08-08，含 HMAC handle 与 topic/queueID 校验用例）；无原型/流程图，自动免除对照 08-08 | — | 源自 M1 最终整支审查。独立持久化密钥（`meta/handle_secret`），鉴权关闭时防伪造也生效。领于 08-07，plan: [m7-batch1](plans/2026-08-07-m7-batch1-hardening-throughput-hygiene.md) |
| B4 | `test/e2e` 拆为独立 Go module | ✅ done(已验) | 中 | [spec](specs/2026-08-07-throughput-and-hygiene-design.md) | — | e2e 独立 module 后主套件与 e2e 均绿（Linux 08-08：主套件 15 包 -race ok，e2e ok 428.6s）；无原型/流程图，自动免除对照 08-08 | — | 源自 M1 最终整支审查。主模块 `go install` 不再拉 SDK 间接依赖。领于 08-07，plan: [m7-batch1](plans/2026-08-07-m7-batch1-hardening-throughput-hygiene.md) |
| B5 | 补两条承重测试：`Scan(limit<=0)` 与并发 `Append` 的 `-race` | ✅ done(已验) | 中 | [spec](specs/2026-08-07-throughput-and-hygiene-design.md) | — | 两条测试入库并随 `-race` 套件全绿（macOS+Linux 08-08）；无原型/流程图，自动免除对照 08-08 | — | 源自 M1 最终整支审查。领于 08-07，plan: [m7-batch1](plans/2026-08-07-m7-batch1-hardening-throughput-hygiene.md) |
| B6 | 混 topic 批次可能在部分落盘后返回不可重试错误 | ✅ done(已验) | 低 | [spec](specs/2026-08-07-auth-and-protocol-hardening-design.md) | — | `SendMessage` 第零遍只读预检入库，rpc 包 -race 20.1s ok（Linux 08-08）；无原型/流程图，自动免除对照 08-08 | — | 源自 M1 最终修复波自查。预检保持无副作用（不 EnsureTopic）。领于 08-07，plan: [m7-batch1](plans/2026-08-07-m7-batch1-hardening-throughput-hygiene.md) |
| B7 | M7：v1.0 发布打磨 | 📦 epic | 高 | — | — | — | — | 08-07 立项定范围：B7.1 多组 AK/SK + B7.2 发布打磨 + 纳入 B1/B3/B4/B5/B6（历史 ID 保留不改号）。进度 6/7，仅剩 B7.2。场景测试计划（plans/2026-08-07-scenario-test.md）独立于 M7 另行执行 |
| B7.1 | 多组 AK/SK | ✅ done(已验) | 高 | [spec](specs/2026-08-07-auth-and-protocol-hardening-design.md) | — | 凭据 list + 严格解析（a4c6e5f：旧 `access_key`/`secret_key` 标量硬报错并给迁移提示）入库，config/rpc 测试随套件全绿（08-08）；无原型/流程图，自动免除对照 08-08 | — | 属 B7。破坏性配置变更已在 v1.0 前窗口完成。领于 08-07，plan: [m7-batch1](plans/2026-08-07-m7-batch1-hardening-throughput-hygiene.md) |
| B7.2 | 发布打磨：docker 镜像、systemd 单元、快速开始、文档总校 | 💡 idea | 高 | — | — | — | — | 属 B7。spec §11 M7 原定内容。放最后做：配置格式与全部能力定稿后只付一次文档成本。注意与 B8（V2）的排序：V2 改协议面与部署形态，文档总校宜在 V2 集成后。<br>**已知待清（08-14 审 B13.4 时撞见，当时刻意没顺手改以免撑大改动面）**：README `## 限制` 章节写着「未实现：多 broker 集群」，是陈旧记述——`internal/cluster` 早已可用（全量测试里跑 81s 全绿，B12 正在修的是它的偶发失败而非「未实现」）。文档总校时一并核对 `## 限制` 与 `## 功能列表` 是否还有类似的滞后表述 |
| B8 | V2：复制层（三节点主备） | 📦 epic | 高 | — | — | — | — | 08-08 立项定范围：纳入 B2（拦截点地基，历史 ID 保留）+ B8.1 spike + B8.2 集群集成 + B8.3 集群场景测试。分片外溢为 B9(V3)。开工前提（已满足）：M7 与写吞吐优化落地 main。**08-10 收口：进度 4/4，B2 / B8.1 / B8.2 / B8.3 全部 ✅ done(已验)，五个批次（batch①地基→②复制内核→③协议面接线→④快照/截断/扩容→⑤场景测试/读屏障/控制台）连同 P0 修复（B10）已全部合入 main**。V2 范围内无未完项；实测暴露但**不在 V2 范围**的两件事各自独立排期：B11（全集群硬宕后从本地日志自恢复）、B7.2（发布打磨与文档总校，需在 V2 定稿后做） |
| B8.1 | etcd/raft 薄壳 spike：最小三节点原型 + 切换/吞吐/追齐实测标定 | ✅ done(已验) | 高 | [spec](specs/2026-08-08-sq-v2-replication-design.md) | — | spike 套件 `-race` 全绿 + 三组数字：切换 1.47~1.53s（`TestKillLeaderFailover`）/ 吞吐两档矩阵（`BenchmarkProposeQuorumFsync`/`Mem`，本机 M1 Pro + Linux 2vCPU，见 spec §2.3）/ 追齐 ≈283ms（`TestUncleanRestartRejoinsAsLearner`） | — | 属 B8。spec §8.1：独立 module 防依赖入主图；产出选举耗时、两档刷盘吞吐、learner 追齐数据回填 spec §2.3/§6。领于 08-08，plan: [v2-batch1](plans/2026-08-08-v2-batch1-replication-foundation.md) |
| B8.2 | 集群模式集成：复制接口、组管理、QueryRoute 路由、切换语义、单机→集群升级 | ✅ done(已验) | 高 | [spec](specs/2026-08-08-sq-v2-replication-design.md) | — | 08-10 合入 main 后整支复验：macOS `go test ./...` 17/17 包 ok（`internal/cluster` 69.6s、`internal/rpc` 26.2s）+ e2e 全量 ok。Linux 双机 e2e 22/22 PASS（联想 4 核 + 阿里云 2 核，三节点 108s / 集群 DLQ 162s / 单机→单节点升级 16s / 扩容 5s / 截断有界 4s），远端零 Go 工具链、交叉编译 broker 经 `SQ_E2E_BROKER` 注入；主套件原生 `-race` exit 0（阿里云 2 vCPU，1:39.63，峰值 RSS 424MB）；386/arm 交叉编译通过。**真机三节点实证**（三台阿里云各一节点）：`/admin/cluster` 的 `self`/`is_leader`/`peers` 跨节点自洽，follower 写转发与 leader 归属一致。跨机写吞吐 3,780/4,509/6,865 msg/s（并发 16/64/256，quorum-mem，失败 0）。无原型/流程图（控制台集群页归 B8.3），自动免除对照 | 08-08 batch②（复制内核）完成：internal/cluster + internal/replication 入库，三节点集成 -race 全绿（macOS+Linux）。08-09 batch③（协议面接线）完成：main 集群装配（ErrUncleanShutdown→Rejoin 自愈）、RouteView 指向组 leader、ForwardAppend/ForwardApply 控制通道、PrepareJoin 重入编排与自动升 voter、单机→单节点集群升级用例、三节点 e2e（kill 数据组 leader→auto-rejoin→升 voter，270 条零丢失）；修复 pebble 批次池内存别名覆写 raft 日志根因（`NewBatchFromRepr` 深拷贝，评审 R0 三节点 e2e 根因）。08-09 batch④（快照+截断+扩容）完成：快照链路（ReadView 一致性视图 + 安装侧清空重来）、周期日志截断（applied−RetainEntries，leader 受 minMatch 下界，并带逃生门：learner 不入下界 + 绝对上限，死节点不再把日志钉死）、单机→多节点扩容 Join（NoBootstrap 空白启动 + BootstrapVoters 种子引导）、三处根因修复（NoBootstrap 撞车 / BootstrapVoters 传输层静默丢弃 / truncateLoop-Done 竞态）、leadership 迁移写屏障（写钩子先于 IsLeader 可见）、ConfChange applied 缺口（ConfState 随 ApplyConfChange 持久化，`raft/<g>/conf`）、rd.MustSync 精准落盘（约 2x 时延收益）、e2e 证据（扩容 200 条快照追齐零丢失 + 截断键数有界；Linux -race 全量绿）。08-10 两轮评审修复入库：快照键族枚举按字节序排序 + 区间两两不相交断言（续扫单调性与块间互斥成强制不变量，堵死多键族死循环/漏发/同键重发）、snapRegistry 借用计数 + 借出续期 + 硬 TTL（GC 不回收在用视图，大库传输不活锁，久悬视图仍可回收）、快照安装失败改 fail-stop + leader 侧停滞检测（ReportSnapshot 让对端回 Probe 重发，不再静默内存/磁盘分叉）、拉取侧游标单调/块数/字节三道守卫、安装期 Step 非阻塞、Join 拒绝判据收窄为「前 raft 期存量标记 + FSM 非空 + 日志未压缩」三者合取（`raft/preraft`，全新集群不再被误拒）、快照视图 TTL 提为配置项 `cluster.snapshot_view_ttl`、控制通道 OpPing=0 保留（探活止步 Manager 自装层，不再穿透业务 handler）。Linux 双机 e2e 全量绿（联想 4 核 + 阿里云 2 核，22/22 PASS：三节点 108s、集群 DLQ 162s、单机→单节点升级 16s、扩容 5s、截断有界 4s）——远端零 Go 工具链，交叉编译 broker 后经 `SQ_E2E_BROKER` 注入（harness 早有此入口，此前误判为「Linux e2e 跑不了」）。08-10 终审第三轮 5 条（判活按 peer 定向台账、安装重试改 2min 时间窗、在途借用判活、maxSnapshotBytes 32 位溢出、fail-stop 恢复路径注释）全部修复：macOS 全绿 + `-race` 全绿，联想 4 核与阿里云 2 核双 Linux 主机 PASS，386/arm 交叉编译通过。08-10 首次端到端写吞吐实测（`test/e2e/sdk_cluster_bench_test.go`，SDK→gRPC→raft→quorum，128B/24 队列/三进程同机）：集群 quorum-mem 3149(2核)/6224(4核) msg/s，quorum-fsync 2111(云SSD)/579(慢盘)，同机单机基线 8422/15221；quorum-mem 为 CPU-bound（核数翻倍吞吐翻倍），quorum-fsync 完全由盘 fsync 延迟决定。跨机三节点未测（只有 2 台可用机器，不给推断数字）。 | 属 B8。spec §3–§7。开工前提：B8.1 数据回填完成，复制接口形状以 spike 实证为准。batch④ plan 已写（08-09，[v2-batch4](plans/2026-08-09-v2-batch4-snapshot-truncation-expansion.md)，10 task）：快照 + 日志截断、单机→多节点扩容、leadership 迁移写屏障（batch③ 评审 m1）、ConfChange applied 键缺口、首轮 leader 日志 term=0、rd.MustSync、confStateFromEntries 多变更守卫、goroutine 泄漏。batch④ **显式外推 batch⑤** 的：B8.3 集群场景测试（kill -9 / 分区 / 滚动重启 / 断电 + 毒消息→DLQ + producer 提交后立退）、admin 控制台集群视图与 follower 写转发 UX、read-index/apply barrier 完整读屏障 |
| B8.3 | 集群场景测试：kill-leader、网络分区、滚动重启、断电模拟 | ✅ done(已验) | 中 | [spec](specs/2026-08-08-sq-v2-replication-design.md) | [原型](../../prototypes/base/pages/cluster.html)（集群页，已与真实页面对照回流 base） | 08-10 batch⑤ 八条场景用例全 PASS（`TestScenario*`：kill -9 leader / 少数派不可写 / 滚动重启 / 全集群断电无法自愈 / 断电 quorum 丢失再恢复 / 毒消息→DLQ / producer 提交后立退 / admin follower 写转发）。**Linux 三条证据线**（08-10）：①主套件交叉编译 17/17 包 ok（联想 4 核，远端零 Go 工具链）②主套件原生 `-race` exit 0、1:39.63、峰值 RSS 424MB（阿里云 2 vCPU，go1.26.1）③e2e 全量 exit 0、18:06.05、峰值 RSS 451MB（联想 4 核，0 个 FAIL）。前端 11 个测试文件 38 条 PASS。**真机三节点实证**（三台阿里云各一节点、内网 172.19.25.178/179/180）：`GET /admin/cluster` 的 `self`/`is_leader`/`peers` 三节点自洽，`peers` 只在该组 leader 上有数据（即控制台「复制进度仅在本节点为该组 leader 时可见」的事实依据）；follower 写转发成立，转发与否与 `/admin/cluster` 报的 leader 归属一致。跨机写吞吐（`TestExternalClusterWriteThroughput`，quorum-mem、20000×128B、客户端与节点1 共置）：并发 16/64/256 → 3,780 / 4,509 / 6,865 msg/s，失败 0，客户端峰值 RSS 120MB | 08-10 batch⑤ 入库（17 提交）：组读屏障原语 + `Manager.ReadBarrier` 批合并 + 接入 `deliver.Receive`；`procCluster` 进程级故障注入 harness（kill/优雅停/原地重启/假死）+ 八条场景用例；`GET /admin/cluster` + `GroupView.Commit` + 控制台集群页（待 apply = commit−applied）+ follower 写转发 UX；原型基准站集群页归位 `pages/` 并全站补入口。另修 `df6848d`：吞吐基准用 `atomic.Value` 记首错，撞多类型 error 会 panic 掉整个基准进程、连已跑完的档位一起丢 | 属 B8。spec §8.2。**订正原措辞**：`test/scenario/` 框架计划（2026-08-07）从未执行，本批不去补它——场景测试长在既有 `test/e2e` 三节点进程级 harness 上（真 broker 进程 + 真 SDK + 真 gRPC），证据更强路径更短。统一不变量对账用确认集（Send 成功即登记，断言全量被消费，允许重复不允许丢失）。batch④（08-09）显式外推三项已全部落地：①本项四类场景 + 毒消息→DLQ + producer 提交后立退 ②admin 控制台集群视图与 follower 写转发 UX ③read-index/apply barrier 完整读屏障。本批实测暴露的 wipe 顺序 P0 见 **B10**（事实只维护一份，此处不复述）；`TestScenarioFullPowerLossCannotRecover` 已随 B10 修复改写为断言「拒启且数据目录保留」。**未测出共置税**：跨机数字有了，但共置矩阵（一台机三 broker 进程）在 2 vCPU/1.6GB 机上因内存不足未跑完，缺同硬件对照，故跨机数字只能单独看 |
| B10 | 不干净关机自愈在取得集群接纳前销毁数据（`Rejoin` wipe 顺序倒置） | ✅ done(已验) | 高(P0) | — | — | `TestRejoinKeepsDataWhenClusterUnreachable` 入库（对端全不可达时 Rejoin 报错 + 数据目录可重开且旧键仍在）；macOS `go test ./...` 全绿；Linux（联想 4 核，6.17.0-29-generic x86_64）`-test.run "Rejoin\|Unclean"` PASS 13.4s、新用例单跑 PASS 3.16s。无原型/流程图，自动免除对照 | 08-10 修复入库（b3cd61e，分支 v2-fix-rejoin-wipe-order）：`Rejoin` 第 2/3 步对调，PrepareJoin 取得接纳后才 WipeForRejoin；编排失败时数据分毫未动，main 改为拒启并在错误串与日志明写恢复途径；harness `rejoinAsLearner` 同步对调 | 08-10 由 batch⑤ 场景四（三节点同时断电）实测暴露、追根因确认。原顺序是「先清空再求接纳」，而 main 在任何 `ErrUncleanShutdown`（kill -9 / OOM / 断电 / 快照安装 fail-stop panic）后无人值守自动走这条路：全集群断电 = 三份副本各自清空后都找不到 leader，永久全损；2-of-3 硬宕 = 两节点清空后再也凑不齐批准自己 ConfChange 的 quorum，一次可恢复事件变成集群永久死亡。`prepareJoinPoll` 只做对外控制调用、不碰 dataDir，两步交换无技术障碍——原顺序是纯设计失误。用户拍板插队修，先于 batch⑤ 收尾。遗留半边由 **B11** 收口（08-10）：能证明本地日志完好时直接原身份恢复，B10 的两条顺序红线断言搬至 `TestScenarioRebootedMemNeedsPermit` 继续生效 |
| B11 | 不干净关机的节点无法从本地日志自恢复，全集群硬宕后集群拒启 | ✅ done(已验) | 高 | [spec](specs/2026-08-10-unclean-local-recovery-design.md) | — | macOS（08-11）：`go test -race ./...` 17/17 包全绿；单测五条恢复分支 + 抬 term 四格 + 安全门守卫 + 签字往返全 PASS；`TestScenario*` 八条全 PASS（B11 四条：full-crash 26.9s / needs-permit 109.2s / grant 13.7s / fsync-resume 27.8s），**两条曾因 B11-OPEN-1 被 skip 的用例已由红转绿，无 skip、无断言弱化**；`TestScenarioFullProcessCrashRecoversLocally` 新增存量断言（断电前后每节点 164→164，零丢失，确认集 328 条全量消费）；Linux（08-11，本地交叉编译后 scp，远端不装 Go 工具链）：`internal/config` / `internal/store` / `internal/cluster` 三个测试二进制全 PASS（0.05s / 2.40s / 79.96s，峰值 RSS 5.7 / 23.2 / 88.6 MiB）；全量 e2e 33 条全 PASS 0 FAIL（20m10s，峰值 RSS 733 MiB）。**双平台验收抓到两处 macOS 照不出的用例缺陷**（均只动 harness，业务断言一条没改）：①重入等待循环用了致命版 `queryRoute`，撞上成员变更引发的选举窗口（QueryRoute 对无 leader 是整体拒答，设计行为）当场 Fatal；②SDK 客户端启动偶发 protobuf size mismatch（golang/protobuf#1609，客户端并发缺陷）打在无重试保护的构造器上。第一版修 ②时只抄了「重试」没抄「重建」，而 SDK 客户端一次性、Start 失败即自我关闭，重试必然拿到 `client has been closed`——由 Linux 上的重试留痕当场证伪后改为重建重试，并与压测路径原有的同款重试合并为一处 | 08-10/08-11 五分支本地恢复落地（分支 v2-b11-unclean-local-recovery）：机器世代读取（Linux boot_id / darwin kern.boottime / 其它平台保守拒读）、raft/bootgen 键、五分支判定纯函数、一次性许可 + ForceLocalRecover、NewManager 接线（local-resume / local-forced 复用 buildGroup clean 分支）、sq recover 子命令、mem 档 local-resume 也抬 term（实现期订正，Pebble NoSync 是异步的）、e2e 四条场景 + restartAll 整批重启 | **状态订正二（08-11 审核，取代下方订正一）**：spec §11 验收第 2 条（静置 ≥500ms 后确认的消息零丢失）不成立的根因已定位并修复——不是 B11 的缺陷，是 `Manager.flusher()` 自 B8.2（`0c7aa8c`，08-08）起就是空操作（见 **B11-OPEN-1**），B11 的场景用例是第一个把它逼出来的。四条验收用例现全绿，已于 08-11 以 `--no-ff` 合入 main（`c39cf92`），分支已删除。~~订正一：代码全部完成但不能记 done，验收第 2 条实测不成立，两条 e2e 场景现为 skip，「验收通过」与「用例被 skip 掉」不能同时为真~~——skip 已删除，断言从未弱化。B10 的遗留半边。**实现期与 plan 预期的两处偏差**（均已并回 plan）：①全集群停机后不能逐台 `pc.restart`（ready 判定依赖 `waitMetaLeader`，首个节点起来时 1-of-3 必死锁），改用 `restartAll` 先全部拉起再逐个等就绪；②Task 6b（mem 档 `local-resume` 也抬 term）与 Task 9（修 flusher 空操作）都是实现期实测推翻 spec 前提后补的订正 task |
| B11-OPEN-1 | mem 档周期刷盘是空操作，quorum-mem 运行期一次盘都不刷（掉电丢已确认消息） | ✅ done(已验) | 高 | — | — | `TestSyncWALPersistsNoSyncWrites`（`internal/store`，`vfs.NewCrashableMem` + `CrashClone` 真掉电语义，带反向对照——不加屏障必须真的丢，否则用例失去判别力）；e2e 新增存量断言（断电前后各读一次 `queues_detail[].next_offset` 求和，恢复后不许比断电前少）；`TestScenarioFullProcessCrashRecoversLocally` 由红转绿：修复前 175→158（三节点同样丢 17）、修复后 164→164 零丢失，确认集 328 条全量消费 | 08-11 修复（分支 v2-b11-unclean-local-recovery）：新增 `store.SyncWAL()`，用 `Batch.LogData`（只进 WAL，不进 memtable/sstable、不被索引）把批次撑成非空再 `pebble.Sync`；`Manager.flusher()` 改调它。周期 200ms、启动条件、「不做成配置项」三者一律不变——不是新机制，是把一个本就声称存在、实际不工作的机制修好 | 08-11 B11 Task 7 实跑暴露，同日定位修复（全过程见 notes/2026-08-11-b11-grant-path-loss.md 第 0 节）。**根因**：`Manager.flusher()` 提交**空批次** + `pebble.Sync`，而 Pebble 的 `commitPipeline.Commit` 开头即 `if b.Empty() { return nil }`——空批次连 WAL 都不写、更不 fsync；叠加 mem 档 `syncPersist` 恒 false，**quorum-mem 档整个运行期一次盘都不刷**，落盘全靠 WAL 缓冲写满自发外刷，断电丢的就是最后那个没写满的 block。**引入者是 `0c7aa8c`（B8.2，08-08），早于 B11 两天；B11 是暴露者不是引入者**——原先「B11 把响亮的拒启换成了安静地少几条」这个取舍问题因此不存在，B11 反而是唯一把它逼出来的用例。**两条方法论教训**：①判据用错代理指标比不验证更坏——此前用「WAL 文件变大」判定 flusher 生效，而 `write(2)` 与 fsync 是两件事，Pebble 的 LogWriter 本就异步写，NoSync 也让文件变大，这个错判把真缺陷盖章成了已验前提（spec §8 已就地留痕）；②拿一条不依赖该机制的路径去为该机制作证是无效对照——「fsync 档不丢」证明不了 flusher 有效，因为 fsync 档靠 `raft.MustSync` 逐次刷盘，根本不需要 flusher |
| B9 | V3：分片集群（topic/queue 分片到多 Raft 组，横向扩展写吞吐） | 💡 idea | 低 | — | — | — | — | 源自 V2 brainstorm（spec §1 非目标表外溢）。前提：B8 的复制组抽象已把每分片=一个复制组的结构预留好，届时只加路由层。单机 57k msg/s 实测在前，无近期压力 |
| B12 | `internal/cluster` 偶发失败——可诊断性修复（不承诺修复偶发本身） | ✅ done(已验) | 中 | [spec](specs/2026-08-14-b12-cluster-flake-diagnosability-design.md) | — | **B12 不因本次关闭**：偶发既未复现也未修复，本次只让下一次失败留得下用例名。08-14 验收：`pebblelog` 三条用例本地独立复跑全过（含 `TestOpenWiresPebbleLogger` 断言 `pebble.Options.Logger` 真接上）；**硬约束核验：`internal/cluster` 零改动**。12 轮复现尝试全 PASS，包耗时 72.401–78.900s，我逐个比对 `/tmp/b12-round-N.log` 原文与表格**完全吻合**。<br>打回一轮，问题全在复现记录而非代码：耗时列混用两种口径（第 1 轮抄 `go test` 包耗时 73.5s，第 2 轮起抄含编译墙钟 108s，而其包耗时实为 77.7s）；「冷 GOCACHE」当时无从取证；第 1 轮与后 11 轮间隔 45 分钟却并列在同一条件描述下；累计 27 轮直接相加。修正后澄清了真实机制——每轮 `GOCACHE=$(mktemp -d)` 独立冷缓存，这正好解释墙钟 108s 与包耗时 77s 的差额（逐轮全量重编译）；**统一口径后第 1 轮 73.5s 与 2–12 轮同区间，没有异常轮**，我此前「第 1 轮没跑在负载下」的推断是混用单位造成的假象。第 1 轮的真实差异是首批命令因后台进程管理缺陷挂起、墙钟未记录、负载存活未取证，已如实写明；负载机制（10 路 `yes > /dev/null`）照实记了「未留脚本、逐轮存活未取证」；累计 27 轮降级为「跨条件粗口径」。<br>**派发两次**：首次交 grok，回合以 `ACP 错误 -32603: Internal error` 异常终止，`resume --force` 后会话卡死（2h 零输出零提交、`attach` render 流沉默），`stop` 后改派 opencode——理由是 spec/plan 早已就绪，这已是 plan 执行而非根因排查。无原型/流程图，自动免除对照。分支 `fix/b12-diagnosability-2` tip `f2de32b`，已 fetch 回本地，**未合并进 main**。复现记录见 [repro-log](notes/2026-08-14-b12-repro-log.md) | 08-14 范围收窄并锁定：brainstorm 后确认本项**不承诺找到那条 flaky 用例**（不可复现、名字不可考，盲目调超时是打补丁）。改为交付「让下一次失败必然可诊断」：Pebble 日志接入项目 slog（根因——`store.Open` 本就收 `*slog.Logger` 却从未转发，`Options.Logger` 为空使 Pebble 退回 DefaultLogger 直写 stderr）+ Makefile 固化输出纪律（`tee` 落盘 + `pipefail` 保退出码）+ 12 轮如实记账的复现尝试。**done 时须拆行**：本行记可诊断性交付，偶发失败本身另起一行保持待观察 | 08-14 领取；派 mac-02 + grok（纯执行 + 反复跑测试，符合派发表画像），基线分支 `base/b12-on-main`。spec §7 有三条外出期间按最保守分支定的**待复核假设**：A1 Pebble `Infof`→Debug 是行为变更（默认 info 级下看不到 compaction 信息）、A2 `Fatalf` 保持 `os.Exit(1)`、A3 生产路径一并接入而非只在测试里静音。<br>**原始记录（08-11）**：08-11 合并 v2-b2-seglog 前的验收中出现：`go test ./...` 冷缓存首跑 `internal/cluster` FAIL（85.057s），此后 **15 次真跑无一复现**——独占 4 次（72–78s）、暖缓存全套 `-count=1` 3 次、独立 GOCACHE 冷缓存全套 2 次（精确重造原失败条件）、8 路 CPU 满载下独跑 6 次，全绿。失败轮比通过轮慢 10%+，形态符合时序敏感用例在 CPU 争抢下超时。**具体是哪条用例未知，且已不可考**：首跑命令写成 `go test ./... \| tail -60`，Pebble 的 `Found 0 WALs` 噪声灌满了那 60 行窗口，把 `--- FAIL: TestXxx` 挤出输出；同一条管道还把退出码变成 `tail` 的 0，几乎让这次失败被整个漏掉。**两条纪律**：①测试输出永不过 `tail`/`head`，要截也先落完整文件再截；②「重跑绿了」不是结论，是又一个数据点——本项正是在 15 次绿之后仍拒绝结案。用户 08-11 拍板：先合（本地 `--no-ff`，`47070c7`，可 reset 撤销），偶发单列待查，不以「已知 flaky」盖章。**下次动 `internal/cluster` 时优先带走**：给该包加 `-timeout` 收紧 + 关掉 Pebble 测试日志噪声，让下一次失败必然留下用例名 |
| B13 | v1.1：官方 SDK 全兼容收口 | 📦 epic | 中 | — | — | — | — | 08-13 立项，源自本日协议面盘点。**盘点结论（事实基线，不要重复盘）**：`MessagingService` 17 个 RPC 已实现 11 个（QueryRoute / Heartbeat / SendMessage / QueryAssignment / ReceiveMessage / AckMessage / ChangeInvisibleDuration / ForwardMessageToDeadLetterQueue / EndTransaction / Telemetry / NotifyClientTermination），未实现 6 个（PullMessage / GetOffset / UpdateOffset / QueryOffset / RecallMessage / SyncLiteSubscription），未实现的由 `internal/rpc/server.go:38` 内嵌的 `UnimplementedMessagingServiceServer` 统一返回 `UNIMPLEMENTED`。**范围**：B13.1 SQL92 + B13.2 PushConsumer 验证 + B13.3 多语言冒烟 + B13.4 RecallMessage + B13.5 AutoRenew + B13.6 RetryPolicy 双重语义，**进度 6/6（宣称范围已满，但 epic 不自动关闭）**（08-13：B13.1 已验收；08-14：B13.2/B13.4/B13.5/B13.6 相继已验收，B13.3 于阿里云三临时机以三门语言实测收口。B13.5/B13.6 是 B13.2 brainstorm 读代码撞出的协议面差异，按分流规则另立，非范围蔓延）。**六条分支全部未合并进 main**，合并次序待定。**为什么不直接把 epic 记 done**：宣称范围 6 项确已全绿，但本 epic 下还挂着后加的 B13.7（上游 Go SDK v5.1.4 自身的数据竞态，sq 侧改不了，属观察项）。按「每片叶子都 done 才允许 epic done」的规则，它是未完成叶子；而它又不是 sq 能靠自己关掉的活。**这个取舍留给用户裁决**：要么把 B13.7 移出本 epic 单列为观察项、本 epic 随即收口，要么维持现状等上游修复。**显式排除、不要再拿出来议**：①4.x remoting 协议（NameServer:9876 + 二进制 remoting）——spec §12 YAGNI 明列「多协议兼容」不做，sq 定位就是 5.x gRPC，存量 4.x 客户端连不上是设计选择不是缺口；②`admin.proto` 的 AdminService 14 个 RPC——那是官方 dashboard/admin 工具的接口，sq 有自建 HTTP Admin API + 内嵌控制台，是替代关系不是缺失；③PullMessage/GetOffset/UpdateOffset/QueryOffset 四件套——PullConsumer 一族，官方 5.x gRPC 客户端并无普及实现，spec §6 已列「v1 不做」，补它是为未来占位而非为兼容现有 SDK。与 **B7.2**（发布打磨与文档总校）的排序：B7.2 宜在本 epic 收口后做，否则「兼容哪些 SDK」这段文档要付两次成本 |
| B13.1 | SQL92 属性过滤 | ✅ done(已验) | 高 | [spec](specs/2026-08-13-sql92-filter-design.md) | — | `go test -race -count=1 ./internal/core/deliver ./internal/metrics ./internal/rpc`（三包全绿）；`cd test/e2e && go test -tags e2e -count=1 -run TestOfficialGoSDKSQL92`（4/4 PASS，123.877s）；无原型/流程图，自动免除对照 08-13 | 08-13 brainstorm 定档三条基准并写入 spec：①兼容基准取官方**文档化子集**，不 bug-for-bug 复刻 Java 实现（怪癖无文档、只能读源码反推，成本高而收益对新建部署有限）②类型**由表达式常量驱动**，解释失败即 UNKNOWN 并按 **SQL 三值逻辑**传播（舍二值逻辑：`NOT (age > 10)` 在 age 缺失时会变 true，把用户明确不想要的消息投出去）③**不加配置开关**，不照抄 RocketMQ 的 `enablePropertyFilter=false`——它默认关的是 ConsumeQueue 侧的 BloomFilter 索引结构，而 sq 的扫描本就要 `DecodeMessage` 解出整条消息，增量成本只是一次 map 查找，两边权衡不是同一件事；防护改由表达式长度/深度/IN 元素数上限兜底 | 属 B13。**唯一确定会被用户撞到的功能缺口**：`internal/rpc/receive.go:77` 对非 `FilterType_TAG` 直接返回 `ILLEGAL_FILTER_EXPRESSION`，Java 的 `new FilterExpression(sql, SQL92)` 现在必挂。sq 主 spec §6 原就排在 v1.1。存量消息与 tag 过滤路径不受影响。**spec 自审抓到的一处真矛盾已修**：接口原定 `Match(m) bool`，与「跳过计数按 FALSE / UNKNOWN 分桶」直接冲突（bool 把两者压成同一个值），已改为三值 `Match(m) Result`——这个分桶是「表达式写错导致静默零命中」唯一的服务端诊断手段，见 spec §7.1。**e2e 不依赖 B13.2**：Go SDK v5.1.4 的 `NewFilterExpressionWithType(expr, SQL92)` 已支持，用现有 SimpleConsumer harness 即可。领于 08-13 计划 [plan](plans/2026-08-13-sql92-filter.md)（7 task）；08-13 派发 mac-02+opencode，分支 feat/sql92-filter，起点 7d63686，task 59ed1aeb。**08-13 交付并验收**（8 commit，29 文件 +2459/−160，末位 e5ca117），已归档、分支同步回本地，**待合并进 main**。**接口化地雷已闭环**：`*TagFilter`→`Filter` 后全仓 80 余处 `Receive(..., nil)` 编译期无感、运行期 panic，靠机械清扫 + `grep -rn "\.Receive(.*, nil)"` 关卡收口；没有加 `filter == nil` 兜底（那会把 `AllPass` 要消灭的 nil 依赖又请回来）。**plan 的三处漏由 executor 终审补上**：布尔量大小比较、`IN` 列表含 NULL、BETWEEN 非数值边界，原先只在求值层兜 UNKNOWN、构建期放行，与 spec §4.2 类型表不符，现改为构建期拒绝 |
| B13.2 | PushConsumer 真实 e2e 验证 | ✅ done(已验) | 高 | [spec](specs/2026-08-13-push-consumer-e2e-design.md) | — | `go test -race -count=1 ./internal/config ./internal/rpc`（全绿）；`cd test/e2e && go test -tags e2e -count=1 -run TestOfficialGoSDKPush`（6/6 PASS，232.96s）——这两条为协调者独立复跑，非执行机自报；执行机全量 e2e `-timeout 60m` PASS（1518s）。无原型/流程图，自动免除对照 08-14 | 08-13 标题去掉「Java」：brainstorm 定档验证载体先用 Go SDK（PushConsumer 路径零覆盖，基础缺陷 Go 一样撞得出来，且复用现有 e2e 模块零新工具链），Java 待本轮跑完再单独决策；08-14 执行期两处承重订正：①用例 6 的「缓存未消费」构造被 SDK drain 语义证伪（worker 收到 shutdown 后 `for t := range tp.tasks { t() }` 排干队列，停机时已投消息全部被消费并 ack，造不出未 ack 缓存，6a 护栏在运行时挡住了它），改测「未 ack 消息在不可见期到期后被重投」，单消费者 + 首投占住唯一消费线程 8s 跨过 5s 不可见期；②终审捞出用例 1b 假绿——静默窗口 15s 短于默认不可见期 1m，而 sq 只有过期重投一条路径，「已 ack」与「ack 全丢」两个世界观测一致、断言恒真，已把该用例不可见期压到 10s | 属 B13。**最大的「不知道」——理论通、从没跑过**：PushConsumer 走的 RPC（QueryAssignment / ReceiveMessage / Ack / ChangeInvisibleDuration / ForwardToDLQ）全部已实现，`internal/rpc/settings.go:120` 也显式下发了 `fifo` / `receiveBatchSize` / `longPollingTimeout`，但 `test/e2e` 只有 Go SDK 且只用了 SimpleConsumer，所以这条消费路径零验证。**08-13 brainstorm 定档三条**：①载体先 Go，Java 另议；②范围「中」——基础闭环 + 长轮询唤醒 + 失败重投 + 超限 DLQ + FIFO 并发顺序证明 + GracefulStop inflight 不丢，共 6 用例，明确排除集群档与 LitePushConsumer；③撞出真缺陷时分流——协商细节类小修内含，架构级另立条目。**已还的欠账**：`settings.go` 的 `fifo` 标志定为**终态 false，不是待翻转的临时值**——原注释理由不完整且暗示翻转是既定方向，方向反了。真实理由有二：顺序安全来自队列级顺序锁（每队列至多一条未终结顺序 inflight），与该标志无关；而翻成 true 会让客户端改建 `FiFoConsumeService`，把**重试计数与 DLQ 判定从 broker 夺给客户端**，与 sq 的 broker 端 maxAttempts/DLQ 设计正面冲突。**08-13 事实订正（原记「Go SDK 没有 PushConsumer」是错的）**：Go SDK v5.1.4 模块里 `push_consumer.go` / `lite_push_consumer.go` 都在，PushConsumer 可以先用现有 Go e2e harness 覆盖，**不必先立 Java 工具链**——本项成本比立项时估的低。Java 用例仍有独立价值（Java 是 RocketMQ 的参考实现，行为差异最先在它身上暴露），但不再是开工前提。另注意 `LitePushConsumer` 依赖 `SyncLiteSubscription` 这个 sq 未实现的 RPC，它不在本项范围，撞到就明确回 `UNIMPLEMENTED`。`settings.go` 里那句「fifo 协商标志待 push 消费流程验证后再翻转」就是本项要还的欠账。**预期**：大概率带出 2–3 个协商细节修正（fifo 翻转、批量与长轮询节拍、顺序消息在 push 下的卡队头语义）。**注意 e2e 形态**：`test/e2e` 是独立 Go module，加 Java 用例需要新建独立目录与工具链约定（别塞进现有 module），且远端 Linux 验证走「本地交叉编译 broker + `SQ_E2E_BROKER` 注入」这条既有路径。**领于 08-13**，计划 [plan](plans/2026-08-13-push-consumer-e2e.md)（6 task）。**spec 写完后自查出一处硬错并已订正**：原 §6.4 断言「`GracefulStop` 不会等阻塞的 listener」，事实相反——`GracefulStop`→`consumerService.Shutdown`→`simpleThreadPool.Shutdown`→`waitGroup.Wait` 是无上限等待，照原构造写测试会**永久死锁**；已改为「缓存未消费」构造（单线程 + 2s 停留让消息滞留 process queue，停机后由同组 B 收走），测的是更真实的停机丢失点。**08-13 派发 mac-02+opencode**，分支 feat/push-consumer-e2e，task 7b9f342c-70b0-4909-b0dd-9e8a12348ee9。**基线特殊**：起点是 `base/b13.2-on-sql92`（= feat/sql92-filter + main 的 B13.2 文档提交，零文件重叠、干净合并、编译通过），因为 B13.1 分支仍未合入 main；main 不动，合并决策仍待定。派发前已在该基线上核对 Task 1/Task 6 的锚点文本唯一且一字不差 |
| B13.3 | Python / C# / C++ SDK 冒烟 | ✅ done(已验) | 中 | — | — | 08-14 阿里云三临时机验收：三门语言各跑「免鉴权往返 + 开鉴权往返 + 错误凭据必拒」，9 项全 PASS。Python `rocketmq-python-client`、C# `RocketMQ.Client` 5.2.1、C++ `apache/rocketmq-clients` cpp（CMake 构建）。**鉴权这项是本条的核心**——此前只是「按官方源码格式对齐、没跑过」，现在三门语言的签名头都被 sq 实际验过，broker 侧有对称证据（`rpc.auth` 打出 `认证失败：AK 或签名不匹配`，method=Telemetry）。错误凭据一项刻意用「已存在且刚成功收发过」的 topic，避免与「topic 不存在」混淆。C++ 那条顺带覆盖了 `ChangeInvisibleDuration`（收 4 条时对一条连改 3 次不可见期，全部正常应答）。无原型/流程图，自动免除对照。**08-15 深水区补验**（超出 B13.3 原范围的加做）：Java（参考实现 5.2.1）10/10 全过；Python 12 项仅 1 红且是新发现的 [B13.8](#)；C# 7 项全过。覆盖 SQL92 三值逻辑/IS NULL/BETWEEN、FIFO 同组同队列与路由非退化（4 队列 topic，10 条按序）、事务显式裁决与孤儿回查、PushConsumer+AutoRenew、定时、Recall（均带对照组）。取证与复跑脚本见 [notes](notes/2026-08-15-multilang-sdk-deep-verification.md)。**C++ 深水区仍未做** | — | 属 B13。**未覆盖（不要误读为已验）**：只做了最小往返，事务 / FIFO / push 消费 / SQL92 过滤在这三门语言上都没验；Java 本就不在本条范围。**三条环境事实，复现时省时间**：①C# 客户端默认走 TLS，sq 是明文 gRPC，不调 `.EnableSsl(false)` 会死在 QueryRoute；②Python 的 `keys` setter 签名是 `(self, *keys)`，property 赋值只能传单值，传 list 会对 list 调 `.strip()` 而抛；③C++ 可用 CMake 构建（Ubuntu 24.04 的 libgrpc++-dev 1.51 + libprotobuf-dev 3.21 够用），但 `protos/` 是 submodule（`--depth 1` 克隆后 `cpp/proto/**` 全是断链软链），且 `find_package(protobuf CONFIG)` 需要一个转接到模块模式的垫片。④`SimpleConsumer` 每次 receive 只轮询一个队列，冒烟 topic 一律预建成 1 个队列。取证见 [four-gaps](notes/2026-08-14-four-gaps-verification.md) |
| B13.4 | RecallMessage：撤回未到期的定时消息 | ✅ done(已验) | 低 | [spec](specs/2026-08-14-recall-message-design.md) | — | `go test ./internal/...` 全绿（18 包，含 cluster 81s）；race 四包全绿（delay 4.1s / rpc 38.2s / produce 6.3s / admin 5.2s）；`build`+`vet` 干净；e2e 用**真实官方 Go SDK v5.1.4**：`TestOfficialGoSDKRecallPreventsDelivery` PASS 18.7s、`TestOfficialGoSDKRecallAfterDeliveryFails` PASS 12.2s（08-14，均本地独立复跑，不采信执行器自报）。**变异验证闸门二（我本地复现）**：把 `moveOne` 的 `else if !ok` 改成 `!ok && false`（唯一变异点），U8 必红、delay 包其余全绿，还原后工作区干净。<br>打回两轮：① **闸门二当时只有注释支撑，没有任何用例会因删掉它而变红**。执行器自己实验发现删重读 U7 仍绿（µs 级窗口 vs 1ms 扫描周期）并主动把 U7 注释从「会红」改成「压力测试而非确定性判别器」——自我修正是对的，但不能停在靠注释论证。补 U8：不赛跑，直接喂 `moveOne` 一份「陈旧 raw + 已被撤回的 key」，精确重放 `Pass` 锁外 `Scan` 拷完快照、`moveOne` 尚未拿锁的那一瞬间。这是闸门二唯一的自动化判据。② README 缺功能列表条目（本仓库惯例：新的对外 RPC 要进 `## 功能列表`），已补。<br>无原型/流程图，自动免除对照。分支 `feat/recall-message` tip `8688427`（8+3 commit），已 fetch 回本地，**未合并进 main**。执行 ledger 见 [ledger](ledgers/2026-08-14-b13.4-recall.md) | 08-14 brainstorm 出 spec，**范围判断被推翻**：原判「工作量小（删个延时键）」不成立。延时调度器是**两段式移入**（先写 `msg/`、再独立批次删 delay 条目，`internal/core/delay/delay.go`），两段之间条目仍在但消息已投出——朴素实现会给出**假成功**（客户端以为撤回了，消费者已收到）。修正后范围：改 `AppendDelay` 签名暴露 seq、新增带域分隔的 recall 句柄族、调度器加**逐条**互斥量、新增 RPC handler、一条并发不变式用例。仍属中等改动，但不是顺手做掉 | 属 B13。**08-14 领取并派发** mac-02 + opencode，分支 `feat/recall-message`，起点 `138db36`（= `feat/auto-renew` 链尾），task `0396a2b5-d1b6-4a52-84cf-540399014ad5`。计划 [plan](plans/2026-08-14-b13.4-recall-message.md)（7 task）。**派发前经审阅 subagent 大改一轮，抓到一条会让整个条目失去意义的设计错误**：原设计的两道闸门（逐条互斥量 + `dueMs>now`）**关不住假成功窗口**——`Pass` 的 `Scan` 在 `moveMu` **之外**，`moveOne` 拿到的是扫描时拷贝的 `raw`、进锁后从不确认条目还在不在。可复现交错：Recall 持锁过判据 → 调度器无锁 Scan 到该条目 → Recall 提交删除并向客户端返回**成功** → `moveOne` 照陈旧 `raw` 入队投递。已补第三道闸门（`moveOne` 拿锁后先 `st.Get(key)` 重读），spec §3.1 同步订正。另订正：域分隔的理由原写「伪句柄会撞上 `delay/{0}{0}`」是**错的**（`DelayKey` 第一分量是 epoch 毫秒，`AppendDelay` 开头就拒 `DeliverAtMs<=0`），真实理由是「不做域分隔时句柄完整性依赖一个下游判据没被改动」。以及一批施工事实错误：fixture 真名 `newFixture`/`newTestProducer`（原写 `newDelayFixture`/`newProducer`）、`store.MsgPrefix` 不存在（未导出）、e2e 是独立 module 且带 build tag（原命令一条用例都跑不到）、`startBroker(t, nil)` 会 panic。spec §7 三条待复核假设，其中 **A2 是必须用户看见的已知限制**：集群档下撤回只在 meta leader 成立（互斥量是进程内的，而调度器只在 meta leader 跑），其他节点回 `HA_NOT_AVAILABLE`；而 SDK 按 topic 路由 RecallMessage，不知道谁是 meta leader，所以集群里会常打到错节点。**单机档功能完整不受影响。** 本设计刻意不新增跨节点转发原语（属架构新增），若要集群可用需另立条目。<br>**原始记录**：属 B13。RocketMQ 5.3+ 新 API，Java SDK 已有 `producer.recall()`；sq 未实现，客户端调用得到 `UNIMPLEMENTED`。sq 已有完整延时消息实现（M3），撤回本质是按 recall handle 删掉尚未到期的延时键 + 处理「已投递/已到期」的竞态返回码，工作量小。优先级低：属于新特性而非存量兼容缺口，没有用户在等<br>**遗留（已实现，非缺陷，供将来参考）**：集群档下 `Recall` 在非 meta leader 上返回 `HA_NOT_AVAILABLE`（`topicErrStatus` 统一分类），而 SDK 按 topic 路由 `RecallMessage`、并不知道谁是 meta leader，故集群下撤回可能需客户端重试。约束的**不是「能不能写」**（raft Propose 在 follower 上会转发），而是「能不能与调度器互斥」——`moveMu` 是进程内互斥量，调度器只在 meta leader 上跑，这点容易看反。与仓库其它 meta-leader 绑定操作处理一致，非本特性引入。e2e 按计划仅覆盖单机档 |
| B13.5 | `AutoRenew`：消费期间自动续租不可见期 | ✅ done(已验) | 中 | [spec](specs/2026-08-14-auto-renew-design.md) | — | 08-14（分支 `feat/auto-renew`，**未合并进 main**）：审核者独立复跑，不采信 executor 自报。`go test -race -count=1 ./internal/core/... ./internal/rpc/... ./internal/config/...` 全 ok（deliver 12.1s / rpc 42.1s / config 6.2s 等）；`cd test/e2e && go test -tags e2e -count=1 -run TestOfficialGoSDKPush` **七条全 PASS**（BasicLoop 67.7s / FIFOOrderLock 29.4s / RetryOwnedByBroker 27.3s / DLQOwnedByBroker 43.3s / LongPollingWakeup 27.2s / RedeliverAfterInvisibleExpiry 35.2s / **AutoRenewPreventsRedelivery 52.3s**，合计 284.8s）。无原型/流程图，自动免除对照。**修复轮抓到并修掉一处假绿**（两个审阅 subagent 合并出的结论）：用例 7 原写法里 `waitCount` 的采集点在 listener 入口、`hold` 之前，`count>=1` 在 t≈0 即满足，12s 静默段实际从 t≈0 起算而非注释声称的 t=20s，post-ack 静默只有约 4s < 5s 不可见期；更深一层是 **7c 根本没有判别力**——若续租污染了 `Attempts`，t=8s 的 ack 会被 `AckBatch` 拒、inflight 留盘，而 `renewable` 四条判据仍全真（归属未变、会话仍活、10m 预算未动），于是无限续租、永不重投，「ack 成功」与「ack 被拒且正被无限续租」两个世界观测完全一致，真正的重投要到 t≈600s。修法必须动**预算**而不只是拉长 sleep：`AutoRenewMaxDuration` 10m→15s + 窗口 12s→25s，健康路径余量 7s、失败路径检出余量 10s | 08-14 brainstorm 定档三条：①**否决「换个更长的常量」**——`auto_renew` 若只映射成一个更大的固定不可见期，慢过它的 handler 照样穿帮，却把所有 push 消费者的崩溃故障转移从 1m 恶化到该常量长度，拿明确的可用性退化换不彻底的兼容改善；②**否决「收件请求到达即续租」**——PushConsumer 本地缓存满（`maxCacheMessageCount`）时会停发 `ReceiveMessage`，而缓存满正是慢消费者场景，靠收件驱动续租等于在最需要它的时候没有触发源；③**采用扫描点续租**——在既有「过期 inflight 重投」判定点分叉，续租发生在本来就要写盘的时刻（零额外写放大）、已持队列锁（零新增并发推理）、持有者一死当场走重投（故障转移不退化）。另定两条实现约束：取件参数走变参 `ReceiveOption` 不改签名（`Receive` 仅 1 处生产调用点却有约 90 处测试调用点，不重蹈 B13.1 清扫成本）；存活判定以闭包随调用传入而非注入 `Deliverer`（避免构造后 setter 留「忘调就静默不续租」的哑火面） | 属 B13。**08-13 由 B13.2 brainstorm 读代码撞出**。PushConsumer 每次 `ReceiveMessage` 请求都设 `AutoRenew: true` 且**不设 `InvisibleDuration`**（SDK `push_consumer.go:734` 的 `WrapReceiveMessageRequest` 只设 LongPollingTimeout/BatchSize/AutoRenew）；语义是「我正在处理，处理期间请自动续租」。sq 全仓对该字段**零业务引用**——只出现在生成的 pb 代码里，`internal/rpc/receive.go:110-112` 直接回落硬编码的 `time.Minute`。**后果**：listener 处理超过不可见时长时 broker 会在客户端仍在处理的情况下重投，而 AutoRenew 的存在正是为了防这个。sq 是 at-least-once，重复投递在契约内，所以**不是正确性缺陷，是与参考 broker 的行为背离**，慢消费者场景会撞到。**为何另立条目而非在 B13.2 内修**：实现要引入租约续期机制（判定客户端是否仍在处理并延展不可见期，需挂在 Telemetry 流或心跳上），属架构级，按 B13.2 spec §9 的分流规则应走完整 brainstorm。定位见 [B13.2 spec](specs/2026-08-13-push-consumer-e2e-design.md) §8.1 **领于 08-14**，spec 见左。**基线必须是 `feat/push-consumer-e2e`（b364deb）**：本项要同步改造 B13.2 的用例 6——PushConsumer 下发 `auto_renew=true`，续租一上线该用例的消息就永不重投、**必红**，这是预期行为变更不是回归（已在 spec §10 与 memory 里各记一份）；改法为该用例 broker mutate 显式关掉续租，另新增用例 7 作为唯一的行为正确性验收（断言 `len(got)==1` 且静默段长于一个完整不可见期，防 1b 那种假绿）。**待用户复核的假设见 spec §12**，最要紧一条是 `auto_renew_enabled` 默认取 `true`——协议正确但改变了每个现存部署的核心投递行为，改回 `false` 的成本是一行 |
| B13.6 | `RetryPolicy` 下发值的双重语义 | ✅ done(已验) | 低 | [spec](specs/2026-08-14-retry-policy-per-client-design.md) | — | `go test -race -count=1 ./internal/rpc/...` => ok 35.9s（08-14，本地独立复跑，不采信执行器自报）。改动面越界检查通过：只 `internal/rpc/settings.go` + `settings_test.go`，`internal/core/deliver` 零改动；`grep -nE '10 \* time\.Second\|5 \* time\.Minute\|RetryBackoffParams'` 无输出（exit=1），确认没人照被推翻的初稿施工。**变异验证（我本地复现，非执行器自报）**：把 `subscribeBackoffPolicy` 的查组分支改成 `ok && false` 后，只有 T7 变红、T1–T6 仍全绿，还原后工作区干净——证明「取组真实值」这条不变式真被钉住。首轮曾打回一次：T3/T5 的夹具让组的 maxAttempts 与 `cfg.DefaultMaxAttempts` 恒等（`EnsureGroup` 写的就是 `meta.New` 收到的值，而夹具让二者同源），实现退化成「完全不查组」也能全绿；补 T7（组在 9 下注册后配置漂移到 5，必须仍下发 9）才补上。无原型/流程图，自动免除对照。分支 `feat/retry-policy-per-client` tip `265631e`，已 fetch 回本地，**未合并进 main** | 08-14 brainstorm 出 spec，**原判断被推翻**：原记「协议只有一个 `BackoffPolicy` 字段，承载不了 RPC 级与消息级双重语义」不成立。查 SDK v5.1.4 两个读取点——`producer_options.go:153`（RPC 级，发送重试）与 `push_consumer_options.go:230`（消息级，nack/DLQ）——分属两类客户端，而 `Settings` 是**逐客户端**协商的，字段本就够用。真缺口是 sq 把 `BackoffPolicy` 赋值放在 `negotiateSettings` 的 switch **外面**（`settings.go:96`），少分了一次岔。条目性质从「评估要不要改」变成「有明确修法的小改动」 | 属 B13。**08-14 领取并派发** mac-02 + opencode，分支 `feat/retry-policy-per-client`，起点 `138db36`（= `feat/auto-renew` 链尾），task `8174e961-f2c9-4c44-99e1-061d3e41a247`。计划 [plan](plans/2026-08-14-b13.6-retry-policy-per-client.md)（2 task）。修法：`backoffPolicy()` 拆成 `publishBackoffPolicy()`（值不变，producer 零变化）与 `subscribeBackoffPolicy(group)`。<br>**派发前经审阅 subagent 推翻了 spec 的核心事实，范围因此大幅收窄——本行原先的记述已作废，勿再照它施工**：<br>① 原记「消费端下发值 `100ms/1s/3` → `10s/5min/组真实值`」**不做**。理由是原论据错了：spec 曾称 `receive.go` 会取 `max(客户端要求, 退避下限)` 把客户端请求静默覆盖，所以下发 broker 的真实退避才「诚实」。核对实现后有两处硬伤——那条覆盖**只用于投递时回填 `InvisibleDuration` 字段**（`toPBMessage`），且判据带 `m.MessageGroup == ""`（**顺序消息被显式排除**）；而 SDK 消费失败走的是 `ChangeInvisibleDuration` → `deliver.ChangeInvisible` → `ExpireAtMs = now + invisible`，**原样透传、一处下限都没有**。真改了的后果与宣称的相反：push 消费失败重投从 100ms 变 10s，顺序消息在反复失败时最坏把队列**队头阻塞 5 分钟**。这是行为退化。<br>② 连带：**不新增 `deliver.RetryBackoffParams` 导出访问器**，`internal/core/deliver` 零改动——那个访问器的唯一用途就是喂上面这个已否决的方案。<br>③ 原记「本条目消除了三条理由中的第二条（两边 maxAttempts 判定不一致），实现时须同步更新 settings.go 那段注释」**是错的**。核对 `feat/auto-renew` 上 settings.go 的实际注释，三条理由里**根本没有「判定不一致」这一条**（实际是：顺序安全不依赖此标志 / 翻 true 会夺走 DLQ 归属权 / B13.7 SDK 竞态）。照原记施工会去划掉一条正确且仍成立的理由。**`fifo` 保持 false，那段注释一个字都不许改。**<br>收窄后本条目**只改 `MaxAttempts` 一个字段**（发布端常量 3/5 → 消费组真实值），回退值取 `cfg.DefaultMaxAttempts` 而非包常量 `meta.DefaultMaxAttempts`（后者是 meta 的兜底 16，组的实际默认值由 main 从配置传入）。**`fifo=false` 下 SDK 不使用 `MaxAttempts`，故存量消费者行为零变化**，改动的意义是让协商描述诚实。spec §6 现有四条待复核假设，新增的 A4 明确记下「退避镜像」这个想法基于错误前提、本条目不做、将来若真要做须另立条目单独评估重投节奏与队头阻塞。<br>**原始记录**：属 B13。**08-13 由 B13.2 brainstorm 读代码撞出**。`settings.go` 的 `backoffPolicy()` 下发的常量按其自身注释是 **RPC 级**重试策略（`backoffInitial=100ms`、`backoffMax=1s`、`backoffMaxAttempts=3`，注释写「单次请求最多尝试次数」），但协议只有一个 `BackoffPolicy` 字段，SDK 把它当**消息级**策略读：`nackMessage` 用它算不可见时长，`eraseFifoMessage` 用它的 `MaxAttempts` 判 DLQ。**当前无用户可见影响**——`fifo=false` 下标准路径不使用 `MaxAttempts`（只 ack/nack），客户端请求的不可见时长（100ms–1s）也会被 broker 的 10s 退避下限兜住。**记录价值在于它是不翻转 `fifo` 的第二条独立理由**：若翻 true，客户端按 `MaxAttempts=3` 放弃并转 DLQ，而 broker 侧 `DefaultMaxAttempts` 默认 16，两边判定不一致。本条目评估是否应下发与消费组真实 maxAttempts 一致的值。优先级低：无当前影响，属语义整洁性。定位见 [B13.2 spec](specs/2026-08-13-push-consumer-e2e-design.md) §8.2 |
| B13.7 | 官方 Go SDK v5.1.4 内部数据竞态：`Start()` 读 `isFifo` 与 telemetry 回调写并发 | 💡 idea | 低 | — | — | — | — | 属 B13。**08-13 由 B13.2 首次给 push e2e 挂 `-race` 撞出**（6 用例 3/6 概率性红，不带 `-race` 6/6 全绿）。`defaultPushConsumer.Start()` 读 `pc.pcSettings.isFifo`（`push_consumer.go:379`）与 telemetry 回调 `applySettingsCommand` 写 `pc.isFifo`（`push_consumer_options.go:226`）无同步并发，双方均为 SDK goroutine，**与 sq 代码无关、sq 侧修不了**。当前无害：sq 下发 `fifo=false` 与 SDK 默认值一致。**但它是「不能把 fifo 协商翻成 true」的第三条独立理由**（已记入 `internal/rpc/settings.go` 注释）——翻 true 也不保证被可靠观测到，消费服务选型会不确定。**待办**：SDK 升版时复查该竞态是否修复；修复后可考虑给 `make e2e` 挂 `-race`（仓库 40 余条既有 e2e 至今从未带过 `-race`）。
| B13.8 | `ReceiveMessageResponse` 缺信封层 `delivery_timestamp`，官方 Python SDK 的 PushConsumer 完全不可用 | ✅ done(已验) | 中 | [spec](specs/2026-08-16-receive-delivery-timestamp-design.md) | — | **08-16 验收，承重判据是真前后对照**（联想真机 100.90.99.61，同机同脚本同 broker 配置，只换二进制）：①**官方 Python SDK 的 PushConsumer 从「listener 永不触发」转为 PASS**——`/root/pyenv/bin/python3 py_deep.py push`，listener 收到 slow-one 第 1 次、45s 处理跨过 30s 不可见期零重投；客户端日志里 `unsupported operand type(s) for -: 'int' and 'NoneType'` 修复后该轮 **0 次**，修复前同一脚本同一机器在 23:32:23 有 1 次。②Python 全套 `py_deep.py` **12 项 0 失败**（08-15 是 12 项 2 失败：push=本条、FIFO=取样预算）。③Java 深水区 `mvn exec:java -Dexec.mainClass=JavaDeep` **10 项 0 失败**——多一帧没打坏参考实现。④Go e2e `cd test/e2e && go test -tags e2e -count=1 -timeout 40m ./...` **48 PASS / 0 FAIL / 2 SKIP**（跳过的两条是需显式开关的吞吐基准），与 08-15 Linux 基线逐条一致。⑤`internal/rpc` 新增 2 条单测（首帧位置 + 取值落在调用时间窗内；空长轮询仍只有一帧 status），各做过变异验证（删发送点 / 取值改定值，都转红）。全量 `go test ./...` 在集成分支上绿。无原型/流程图，自动免除对照。**未覆盖**：C# 未回归（环境在已失联的阿里云机上，联想机无 .NET），风险判断见 [notes](notes/2026-08-15-multilang-sdk-deep-verification.md) 的「已修复」一节 | — | 属 B13。领于 08-16，plan 见 [plans](plans/2026-08-16-b13.8-receive-delivery-timestamp.md)。**修复在分支 `fix/b13.8-receive-delivery-timestamp`（`ab0cce9`），未合 main**；另有 `verify/final-integration-3` = integration-2 + 本条。**08-15 多语言深水区验证时发现，是真缺陷不是用例问题。**根因：`proto/apache/rocketmq/v2/service.proto:105` 的 `ReceiveMessageResponse` 是 `oneof content { status | message | delivery_timestamp }`，第三个分支的注释写明 「The timestamp that brokers start to deliver status line or message」——**该由 broker 发**。sq 从不发这一帧：`ReceiveMessageResponse_DeliveryTimestamp` 在仓库里只出现于生成的 pb 代码，`internal/rpc/receive.go` 零引用（sq 只填了 per-message 的 `SystemProperties.DeliveryTimestamp`，那是延时消息的到期时间，两回事）。**后果是链式的**：Python SDK 的 `consumer.py:225` 因字段缺失不给 `msg.transport_delivery_timestamp` 赋值 → `client_metrics.py:151` 的 `latency = current - transport_delivery_timestamp` 抛 `unsupported operand type(s) for -: 'int' and 'NoneType'` → 异常被 `push_consumer.py:266` 的 `__handle_received_message` 吞掉 → 同一 try 块里的 `cache_messages` 与 `execute_consume` 都不执行，**listener 永远不触发**，消息卡在 in-flight 并进入无限重试。**为什么至今不可见**：Go SDK（`simple_consumer.go:257`）、C# 与 **Java（参考实现，5.2.1）** 都只是把该字段存进变量、容忍缺失。三者的 PushConsumer 08-15 均实测正常（AutoRenew 一并验过）。**定级依据**：08-15 补测 Java 后确认参考实现不受影响，故优先级由「高」下调为「中」——不是「协议实现整体错」，而是「四门 SDK 里 Python 这一门用不了」。**影响面**：Python 用户的 push 消费完全不可用；Python 的 SimpleConsumer 不受影响（不走 `receive_after`）；Java / Go / C# 全部不受影响。**修法预计极小**：在 `receive.go` 投递消息前后补发一帧信封层 `&pb.ReceiveMessageResponse{Content: &pb.ReceiveMessageResponse_DeliveryTimestamp{...}}`。取证：`172.19.25.178:/root/logs/rocketmq_python/rocketmq_client.log`、[notes](notes/2026-08-15-multilang-sdk-deep-verification.md) |

| B14 | `store.OnApplyObserve` 集群档启动期数据竞态 | ✅ done(已验) | 高 | [spec](specs/2026-08-14-apply-observer-race-and-recovery-log-level-design.md) | — | 08-14 验收（审核者本地独立复核，不采信执行器自报）：①改动面 9 文件 = 白名单 8 + ledger，零越界；`OnApplyObserve` 仅剩 4 处注释提及、零代码引用——**没有留兼容别名**（这是本条最担心的失败模式）。②两条承重判别器由我亲手变异验证：退回裸全局 → `WARNING: DATA RACE`，栈精确指向 `observeApply`（读）/ `SetApplyObserver`（写），与 ledger 记录的调用链逐行吻合。③本地 `go test -race ./...` 18/18 包 PASS、零 race。④**带 `-race` 的真 broker 跑全量 e2e**（B14 的完整验收，单测判别器证不了集群档启动路径）：34 PASS / 2 SKIP，`WARNING: DATA RACE` 归零，`exit status 66` 零命中，此前被它连带判失败的 11 条 `TestScenario*` 全部转绿。⑤**08-15 Linux 真机复验（集成基线）**：把修复并入含全部 12 条特性分支的 `verify/final-integration-2` 后，在 Ubuntu 24.04 上 `go test -race ./...` 18/18 包 ok 零 race；race broker 跑 e2e **48 PASS / 0 FAIL / 2 SKIP**（2 条 SKIP 需 `SQ_BENCH=1`），DATA RACE 与 `exit status 66` 均为 0，11 条 `TestScenario*` 全绿；跨物理机三节点集群五项全过、三机零 panic 零 race。合并只撞一处机械冲突（`collector.go` 的 `NewRegistry` 签名 vs 注释），`group_test.go` 原先读写裸全局那段已被正确迁到 `SwapApplyObserver`、与 seglog/apply 合批共存，`go vet` 含测试文件编译全过。取证见 [notes](notes/2026-08-15-integration-baseline-linux-verification.md)。此前记为「负载离群值」的 `TestOfficialGoSDKDelayRestartRecovery` 在 Linux 直接绿（14.21s），支持该判断。无原型/流程图，自动免除对照 | — | 08-14 领取，与 B15 合并一份 spec/[plan](plans/2026-08-14-b14-b15-apply-observer-and-log-level.md)，基线 `main`，分支 `fix/b14-b15-observer-and-loglevel` tip `dbb2d03`，**未合并进 main**。**派发两次**：首轮 opencode server 被 mac-02 的进程围栏挤掉（另一会话的任务占 1827 进程），零提交；`continue` 同会话续接后完成。**plan 缺陷一处（我的）**：判别器写侧原写成 `Set(fn)` 紧跟 `Set(nil)`，非 nil 窗口只有相邻语句的纳秒级间隙，而读侧要等一次微秒级 Pebble 提交才读钩子——整包跑 11/11 红、单跑 20/20 绿。执行器带完整证据上报而非擅改断言，经裁决改为「每轮换新非 nil 闭包、循环后统一清、10s 预算兜底」并重做变异验证，plan 已同步订正（`0ede7da`）。**遗留观察**：`TestOfficialGoSDKDelayRestartRecovery` 在 race 插桩 + 高负载下停机 6.03s 超 5s 预算（隔离复跑 5/5 绿、稳定 1.01s，判为负载离群值）；该预算在 race 档偏紧，与 B12 那条偶发失败同类。**另一处 plan 疏漏（我的）**：`store.go` 三处口径注释仍写着已不存在的 `OnApplyObserve` 符号，plan 明写「保留不动」故执行器照办，合并时顺手改 |08-14 领取，与 B15 合并一份 spec/[plan](plans/2026-08-14-b14-b15-apply-observer-and-log-level.md)，基线 `main`。**已定不要再议**：走 `atomic.Pointer`，不走「只修 main.go 顺序」（那条跨文件顺序契约今天已被证明守不住）也不走「改 `store.Open` 签名」（改动面与收益不成比例）。08-14 由「broker 带 `-race` 跑 e2e」挖出，单测 `-race` 全绿没能发现。**竞态**：包级钩子 `store.OnApplyObserve`（`internal/store/store.go:31`）被 main goroutine 的 `metrics.NewRegistry`（`collector.go:190`，经 `main.go:389`）写，同时被 raft apply goroutine 的 `store.ApplyWith`（`store.go:205` ← `cluster/group.go:1506`）读。**根因是启动顺序违反了代码自己写下的契约**：`main.go:385-388` 明确要求 metrics registry 先于任何后台 goroutine 装配，但那段推理只点了 retention/delay，漏了集群档的 apply goroutine——`m.Start` 在 `main.go:299` 就把它们拉起来了，比 `NewRegistry` 早 90 行。单机档无此路径。**修复不是一行挪动**：`NewRegistry` 依赖的 `mt/tx/srv` 都在 `cluster.NewManager` 之后才构造。建议改成 `atomic.Pointer[func(time.Duration)]`（读路径一次原子 load）——顺序契约已被打破过一次，说明它守不住。**连带影响**：race detector 让 broker 以 `exit status 66` 退出，e2e 里 10 条集群/场景用例因 harness 断言「SIGTERM 后干净退出」而失败，全是这一个 bug 的下游。修完值得重跑一轮 race e2e，那才是「集群档零 race」的完整证据 |
| B15 | 正常恢复路径打 ERROR 日志（`不干净关机后本地恢复`） | ✅ done(已验) | 低 | [spec](specs/2026-08-14-apply-observer-race-and-recovery-log-level-design.md) | — | 08-14 验收：我亲手做变异验证——把分级改回一律 Error，`TestLocalResumeBumpLogsWarn` 必红并报「级别是 ERROR，期望 WARN」；`TestForcedRecoverBumpLogsError` 仍绿（两条成对存在的设计成立）。既有的 `TestForceLocalRecoverBumpsTermAndConsumesPermit` 等行为用例未被破坏。**08-15 真实部署前后对比**（同一场景：跨物理机三节点、`kill -9` node1 再拉起，每组一条共 4 条）：修复前 4 条 `level=ERROR`，修复后 4 条 `level=WARN`，node1 整个进程生命周期 ERROR 计数为 0。且非碰巧命中——恢复路径判定日志给出 `recovery=local-resume`、`mode=ack-quorum-mem`、`bootgenNow == bootgenStored`、`permit=false`，正是 `needsTermBump(pathLocalResume, AckQuorumMem)` 为真的那条路；ERROR 档的 `ForceLocalRecover` 本轮未触发，符合预期。取证见 [notes](notes/2026-08-15-integration-baseline-linux-verification.md)。无原型/流程图，自动免除对照 | — | 08-14 领取，与 B14 同一份 spec/plan 与分支。按调用方分级：`BumpTermsForLocalResume` → WARN（mem 档投票走 NoSync 的预期动作，代价只是多一轮选举）、`ForceLocalRecover` → ERROR（`--grant` 签字放行，可能丢已确认消息）。**已知局限（我 plan 留的洞）**：判别器的 `levelOf` 只取第一条匹配记录，若将来退化成「首组 WARN、后续组 ERROR」抓不到；实际风险低（循环里只有一条日志语句、reason 是常量，无按组分叉路径），执行器守纪律未擅改，记账不修 |08-14 领取，与 B14 同一份 spec/[plan](plans/2026-08-14-b14-b15-apply-observer-and-log-level.md)。**已定不要再议**：按调用方分级（local-resume→WARN、`--grant` 签字放行→ERROR），不一刀切降 WARN——`bumpTermsInto` 被两个语义差一个数量级的调用方共用。08-14 跨机集群验证时发现：`kill -9` 后重启每次打 4 条（每组一条）`level=ERROR msg="不干净关机后本地恢复：任期已抬、投票已清"`（`raftstore.go:1191`）。走的是 `pathLocalResume` + quorum-mem，是 `manager.go:466` 注释写明的**预期路径**（mem 档投票走 NoSync 可能没落盘，所以抬任期），后果只是多一轮选举。把 documented 的正常恢复打成 ERROR 会训练运维忽略 ERROR——正是 B12 关心的那类可诊断性问题。建议降为 WARN。改动面极小，适合和 B14 一起做 |