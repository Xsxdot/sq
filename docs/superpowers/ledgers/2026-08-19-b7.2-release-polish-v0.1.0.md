# B7.2 发布打磨 v0.1.0 执行 ledger

| 时间 | 范围 | 结果 |
|---|---|---|
| 2026-08-19 | 基线 | `go test ./...` 首次因默认 `/root/go/pkg/mod` 只读失败；改用任务临时 `GOMODCACHE`/`GOCACHE` 后完整通过。 |
| 2026-08-19 | Task 1，commit `c68fd27` | 双裁决通过：Apache-2.0 正文逐字对照、版权行已替换、201 行；`git diff --check` 通过。 |
| 2026-08-19 | Task 2，修复轮 1，范围 `c68fd27..HEAD` | 机械替换后 `internal/rpc` protobuf raw descriptor 的 wire 长度未同步，原始失败为 `slice bounds out of range [-4:]`；同步三份生成描述符长度并更新 `buf.gen.yaml` 后，定向 rpc 测试通过。 |
| 2026-08-19 | Task 2，待提交范围 `c68fd27..HEAD` | 双裁决通过：旧模块路径在所有 `*.go`/`*.mod` 中归零；build/vet/全量 Go 测试/e2e 编译均退出 0；仅路径与生成描述符元数据变化。 |
| 2026-08-19 | Task 3，待提交范围 `5b7d36e..HEAD` | 双裁决通过：版本默认值、ldflags 注入、三行换行输出、配置加载前分支及两条启动日志均符合规格；版本单测、cmd 全测、实际二进制与 recover 分流验证通过。 |
| 2026-08-19 | Task 4，待提交范围 `9fe7e0e..HEAD` | 双裁决通过：systemd 路径/超时/加固与 install.sh 的错误输出、成功日志、不自动启动、配置不覆盖均符合规格；`bash -n` 通过、脚本可执行。`systemd-analyze verify` 退出 1，原始输出：`sq.service: Command /usr/local/bin/sq is not executable: No such file or directory`，真机安装前未验证。 |
| 2026-08-19 | Task 5，待提交范围 `3acd162..HEAD` | 双裁决通过：workflow 仅 v* tag 发布、手工触发仅 artifact、Node 22/Go 1.26.1、三平台与前端构建均符合规格；YAML 通过，本地三包构建成功，HTML 痕迹计数 2。首次 npm 缓存只读失败，改任务缓存后通过；npm 报 8 个漏洞留终审 triage。 |
| 2026-08-19 | Task 6，待提交范围 `e281f31..HEAD` | 双裁决通过：功能、配置、Admin API、页面数及三处已知陈旧声明均逐条回指代码/测试；实际功能项 15 条、页面 12 个及缺失 cluster 配置已记录；未修业务代码，`git diff --check` 通过。 |
| 2026-08-19 | Task 7，待提交范围 `9138946..HEAD` | 双裁决通过：README 重写为 7 节/100 行，四份 docs 分别覆盖配置、Admin API、消息语义和部署；源码 Node/空控制台陷阱、SDK 缺口、协议边界、集群 Recall 限制均按核对表落笔；链接、公开里程碑扫描、复核勾选和 `git diff --check` 通过。 |
| 2026-08-19 | 终审修复轮 1，待提交范围 `a24fdaa..HEAD` | 发现核对表 F2/F3 引用了不存在的 `internal/core/filter`；改为实际的 `internal/core/deliver/filter.go` 与 `sql92_*`，未改变业务代码或对外结论。 |
| 2026-08-19 | 终审复审，范围 `900a64d..d994549` | 完整分支 diff 复审通过；Go build/vet/全量测试、e2e 编译、前端构建、三平台出包 dry-run、版本默认/注入输出均成功；旧模块路径 0、未提交 `web/dist` 产物、公开文档约束和链接检查通过。 |
| 2026-08-19 | 文风复审，待提交范围 `477bac7..HEAD` | 仅收紧 README：移除结构性标题、重复说明和填充句；功能表后保留单一文档指路，未改事实或拆分文档；断链 0、里程碑编号 0、README 84 行。 |
